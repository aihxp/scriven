# 37-02 Summary

The alias policy is now explicit: adapted names are descriptive labels unless a runtime intentionally installs wrappers. Source docs, help text, and router guidance now use canonical runnable commands and describe adapted labels in prose, avoiding unsafe wrapper semantics for colliding names like `chronology` and `doctrinal-check`.
