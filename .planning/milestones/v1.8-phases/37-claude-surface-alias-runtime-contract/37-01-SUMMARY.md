# 37-01 Summary

Claude-facing runtime docs now align on the flat `/scr-*` contract. `README.md` shows `/scr-autopilot` for Claude Code, and `docs/architecture.md` now documents `~/.claude/commands/scr-*.md` instead of the stale nested `scr/` layout.
