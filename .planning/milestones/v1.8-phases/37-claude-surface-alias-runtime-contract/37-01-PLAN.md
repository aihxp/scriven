# 37-01 Plan

## Goal

Normalize Claude-facing examples and layout docs around the flat `/scr-*` contract.

## Tasks

1. Update runtime docs that still describe the old nested Claude path.
2. Ensure primary user-facing examples keep Claude on `/scr-*`.
3. Add a Claude flat-surface example for autopilot-style flows.

## Verification

- Runtime docs describe Claude command files as `scr-*.md`.
- README shows `/scr-autopilot` for Claude Code.
