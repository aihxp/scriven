# 37-02 Plan

## Goal

Lock the adapted-alias contract without adding unsafe wrappers.

## Tasks

1. Audit adapted rename targets from `CONSTRAINTS.json`.
2. Confirm collision risks around `chronology` and `doctrinal-check`.
3. Rewrite source docs to present adapted names as labels, not runnable `/scr:*` commands.

## Verification

- Source docs no longer advertise adapted labels as runnable `/scr:*` commands.
