# 36-02 Plan

## Goal

Apply the canonical inventory policy to installer and manifest generation.

## Tasks

1. Rework manifest generation to derive rows from the file-backed command inventory.
2. Preserve categories/descriptions from `CONSTRAINTS.json` without reintroducing phantom sacred commands.
3. Add regression coverage for the manifest contract.

## Verification

- The generic manifest includes `/scr:sacred:concordance`.
- The generic manifest does not include phantom `/scr:concordance`.
