# 38-03 Summary

Contributor and runtime-facing docs now state the naming contract more clearly: Claude uses flat `/scr-*`, generic slash-command runtimes use `/scr:*`, Codex uses `$scr-*`, and adapted labels in source docs are descriptive unless a runtime deliberately installs wrappers.
