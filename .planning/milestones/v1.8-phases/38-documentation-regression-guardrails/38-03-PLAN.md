# 38-03 Plan

## Goal

Document the runtime naming contract clearly for contributors and runtime-facing docs.

## Tasks

1. Refresh architecture/runtime docs for Claude’s flat `/scr-*` surface.
2. Keep README examples aligned to runtime-native invocation styles.
3. Record the no-blanket-wrapper alias decision in the phase summaries/context.

## Verification

- Runtime-facing docs distinguish Claude `/scr-*`, generic `/scr:*`, and Codex `$scr-*`.
