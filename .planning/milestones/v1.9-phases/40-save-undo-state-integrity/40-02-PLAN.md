# 40-02 Plan

## Goal

Repair the save and undo command contracts so they create clean, correctly targeted checkpoints.

## Tasks

1. Update `save.md` so the `STATE.md` action row is written before staging and committing `.manuscript/`.
2. Update `undo.md` so it identifies the explicit `.manuscript/` target commit instead of assuming `HEAD`.
3. Make the undo flow use one final commit that includes both the revert and the `STATE.md` update.

## Verification

- Save/undo command prose now guarantees a clean worktree after success and names the correct revert target.
