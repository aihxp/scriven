# 40-03 Plan

## Goal

Lock the repaired save/undo sequencing with regression coverage and verification.

## Tasks

1. Add focused tests that fail if save updates `STATE.md` after committing.
2. Add focused tests that fail if undo reverts `HEAD` instead of an explicit manuscript commit.
3. Run the targeted save/undo tests plus the full suite.

## Verification

- Tests fail if save/undo sequencing drifts back to dirty-tree or wrong-target behavior.
