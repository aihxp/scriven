# 39-03 Plan

## Goal

Lock the repaired draft-path contract with docs alignment and regression coverage.

## Tasks

1. Update any trust-facing docs/examples in scope that still teach the conflicting root-level draft layout.
2. Add regression checks that fail if producer and consumer commands drift apart again.
3. Run the relevant targeted test suite plus full `npm test`.

## Verification

- Tests fail if canonical source draft paths diverge across producer/consumer command contracts.
- Full test suite remains green after the contract sweep.
