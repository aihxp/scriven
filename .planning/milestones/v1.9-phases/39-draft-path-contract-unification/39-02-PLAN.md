# 39-02 Plan

## Goal

Repair producer and consumer commands so they all use the canonical source draft path.

## Tasks

1. Update producer commands such as `draft.md` and `import.md` to write the canonical body-draft path.
2. Update source-text readers such as `back-translate.md` to read the same canonical path.
3. Sweep any nearby trust-facing command prose that still hard-codes the conflicting root-level contract.

## Verification

- No active-manuscript command in the Phase 39 scope writes root-level `.manuscript/{N}-{A}-DRAFT.md` files as the primary contract.
