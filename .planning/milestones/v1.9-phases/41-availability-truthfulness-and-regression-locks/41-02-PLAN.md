# 41-02 Plan

## Goal

Repair help, trust docs, and command prose so they no longer advertise unavailable commands or unsupported adapted labels.

## Tasks

1. Update `help.md` so it filters by group availability plus narrower constraints like `nonfiction_only` and `comic_only`.
2. Remove or reword command-file claims that hidden commands “appear as” unsupported adapted labels.
3. Fix any trust-facing docs that currently imply subgroup-only commands are available to the entire parent group.

## Verification

- Help/docs/commands no longer promise unavailable commands for sacred, academic, or non-comic visual projects.
