# 41-03 Plan

## Goal

Lock the repaired availability contract with regression tests and milestone verification.

## Tasks

1. Add tests derived from `CONSTRAINTS.json` that fail on unsupported adapted-label promises.
2. Add explicit checks for constrained help/docs language, including `nonfiction_only` and `comic_only`.
3. Run targeted availability tests plus the full suite.

## Verification

- Tests fail if help/docs/command prose drift back to broad-only availability logic or unsupported adapted-label promises.
