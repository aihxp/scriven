# 42-03 Plan

## Goal

Lock the cover asset contract so future edits cannot collapse it back into one ambiguous "cover file" model.

## Tasks

1. Add a focused regression test for the canonical cover asset paths.
2. Assert source-file and prompt-output boundaries directly in the test.
3. Run targeted and full-suite verification.

## Verification

- A dedicated phase test fails if canonical cover asset paths or source/staging boundaries drift.
