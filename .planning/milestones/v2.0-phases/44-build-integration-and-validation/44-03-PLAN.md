# 44-03 Plan

## Goal

Lock the integrated cover workflow with phase-specific regression coverage and full-suite verification.

## Tasks

1. Add a build-integration test covering build/export/reference surfaces.
2. Extend trust-surface checks to cover command-reference and shipped-assets cover language.
3. Run targeted verification and the full `npm test` suite.

## Verification

- Phase-specific tests fail on stale cover paths, stale trust-surface language, or missing canonical asset references.
