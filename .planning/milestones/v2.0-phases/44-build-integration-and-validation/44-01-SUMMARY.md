# 44-01 Summary

The build and export commands now point at the canonical `.manuscript/build/` cover assets instead of the legacy output cover path. EPUB embedding uses the ebook asset, and print workflows now reference the paperback and hardcover PDFs directly.
