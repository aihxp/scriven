---
phase: 09-generic-platform-support
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - test/installer.test.js
autonomous: true
requirements:
  - PLAT-06

must_haves:
  truths:
    - "Tests validate that skill-file runtimes have skills_dir_global and skills_dir_project properties"
    - "Tests validate that generateSkillManifest produces a manifest with all commands from CONSTRAINTS.json"
    - "Tests validate the generic runtime entry exists with type 'skills' and detect returning false"
    - "Tests validate the full skill-file install flow writes SKILL.md and command files to a temp directory"
    - "Existing installer tests continue to pass unchanged"
  artifacts:
    - path: "test/installer.test.js"
      provides: "Test suite covering generic SKILL.md installer path"
      contains: "generateSkillManifest"
  key_links:
    - from: "test/installer.test.js"
      to: "bin/install.js"
      via: "require and invoke generateSkillManifest, RUNTIMES"
      pattern: "require.*install"
---

<objective>
Add tests validating the generic SKILL.md installer path: runtime type classification, SKILL.md manifest generation, Manus skill-file routing, and the Generic option. Tests run alongside the existing command-directory tests.

Purpose: Ensure the new skill-file installation strategy works correctly and doesn't break existing command-directory installs.

Output: Updated `test/installer.test.js` with new test blocks for PLAT-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-generic-platform-support/09-CONTEXT.md
@.planning/phases/09-generic-platform-support/09-01-SUMMARY.md

@bin/install.js
@test/installer.test.js
@data/CONSTRAINTS.json
</context>

<interfaces>
<!-- Exports from bin/install.js after Plan 01 -->
From bin/install.js (after Plan 01):
```javascript
module.exports = { copyDir, RUNTIMES, generateSkillManifest };

// RUNTIMES entries now have:
// - type: 'commands' | 'skills'
// - For 'commands': commands_dir_global, commands_dir_project, agents_dir_global, agents_dir_project, detect()
// - For 'skills': skills_dir_global, skills_dir_project, detect()

// generateSkillManifest(constraintsPath: string): string
// Returns markdown manifest listing all /scr:* commands
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test suite for generic SKILL.md installer path</name>
  <files>test/installer.test.js</files>
  <read_first>test/installer.test.js, bin/install.js</read_first>
  <behavior>
    - Test: Every RUNTIMES entry has a `type` property that is either 'commands' or 'skills'
    - Test: Command-directory runtimes (claude-code, cursor, gemini-cli, codex, opencode, copilot, windsurf, antigravity) have type 'commands'
    - Test: Command-directory runtimes have commands_dir_global, commands_dir_project, agents_dir_global, agents_dir_project
    - Test: Skill-file runtimes (manus, generic) have type 'skills'
    - Test: Skill-file runtimes have skills_dir_global and skills_dir_project properties
    - Test: Generic runtime has label 'Generic (SKILL.md)' and detect() returns false
    - Test: Generic runtime is the last entry in RUNTIMES
    - Test: Manus runtime has type 'skills' and detects via ~/.manus/ or Manus.app
    - Test: generateSkillManifest returns a string containing '# Scriven'
    - Test: generateSkillManifest output contains markdown table header '| Command | Category | Description |'
    - Test: generateSkillManifest output includes /scr:new-work, /scr:draft, /scr:help entries
    - Test: generateSkillManifest output includes sacred commands like /scr:sacred:concordance
    - Test: generateSkillManifest output has at least 80 command rows (pipe-delimited lines with /scr:)
    - Test: Skill-file install flow creates SKILL.md in target directory (using copyDir + fs.writeFileSync with generateSkillManifest output to a temp dir)
    - Test: Existing copyDir and RUNTIMES tests still pass (no modification to existing tests)
  </behavior>
  <action>
Add three new `describe` blocks to test/installer.test.js, after the existing blocks. Import `generateSkillManifest` from the install module.

1. **Update the require line** at the top:
   ```javascript
   const { copyDir, RUNTIMES, generateSkillManifest } = require('../bin/install.js');
   ```

2. **Add `describe('RUNTIMES type classification', () => { ... })`:**
   - Test every entry has `type` property of 'commands' or 'skills'
   - Test the 8 command-directory runtimes have type 'commands' and required dir properties
   - Test manus and generic have type 'skills' and `skills_dir_global`, `skills_dir_project` properties
   - Test generic has `detect` that returns `false`
   - Test generic is last key in `Object.keys(RUNTIMES)`
   - Test generic has label 'Generic (SKILL.md)'

3. **Add `describe('generateSkillManifest', () => { ... })`:**
   - Call `generateSkillManifest(path.join(ROOT, 'data', 'CONSTRAINTS.json'))` once in a variable
   - Test output is a string
   - Test output contains '# Scriven' header
   - Test output contains '| Command | Category | Description |' table header
   - Test output includes '/scr:new-work'
   - Test output includes '/scr:draft'
   - Test output includes '/scr:help'
   - Test output includes '/scr:sacred:concordance' (sacred subcommand)
   - Test output has at least 80 lines containing '| `/scr:' (command rows)
   - Test output includes category names from CONSTRAINTS.json (core, navigation, quality, etc.)

4. **Add `describe('Skill-file install simulation', () => { ... })`:**
   - Create a temp directory
   - Call `generateSkillManifest()` and write to `tmpDir/SKILL.md`
   - Call `copyDir(path.join(ROOT, 'commands', 'scr'), path.join(tmpDir, 'commands', 'scr'))`
   - Verify SKILL.md exists and has content
   - Verify command files exist in subdirectory (e.g., `commands/scr/demo.md`)
   - Clean up temp directory in finally block

5. **Do NOT modify existing test blocks** -- only add new ones.

Use `node:test` describe/it pattern matching the existing test file style. Use `assert.ok` and `assert.equal` from `node:assert/strict`.
  </action>
  <acceptance_criteria>
    - grep -q "generateSkillManifest" test/installer.test.js
    - grep -q "type classification" test/installer.test.js
    - grep -q "Skill-file install" test/installer.test.js
    - grep -q "generic" test/installer.test.js
    - grep -q "/scr:sacred:" test/installer.test.js
  </acceptance_criteria>
  <verify>
    <automated>/opt/homebrew/bin/node --test test/installer.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All existing installer tests pass (copyDir, RUNTIMES entries)
    - New type classification tests pass for all 10 runtimes
    - generateSkillManifest tests pass verifying content and completeness
    - Skill-file install simulation test passes (SKILL.md + command files written correctly)
    - Test file uses node:test style consistent with existing tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test temp dirs | Tests create and clean up temporary directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | T (Tampering) | test temp dirs | accept | Tests use os.tmpdir() and clean up in finally blocks; standard test pattern |
</threat_model>

<verification>
- `node --test test/installer.test.js` passes all tests (existing + new)
- New tests cover: type classification, manifest generation, skill-file install simulation
- No existing tests modified or broken
</verification>

<success_criteria>
- Test suite validates the complete generic SKILL.md installer path
- All 10 runtimes validated for correct type and properties
- generateSkillManifest output verified for completeness and format
- Full install simulation proves SKILL.md + command files are created correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-generic-platform-support/09-02-SUMMARY.md`
</output>
