---
phase: 16-trust-regression-coverage
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - test/package.test.js
autonomous: true
requirements:
  - QA-02
user_setup: []
must_haves:
  truths:
    - A maintainer can verify packaged npm contents include the proof bundles promised on the launch surface.
    - A maintainer can verify packaged npm contents include the currently shipped export templates listed in docs/shipped-assets.md.
    - Package dry-run coverage remains part of npm test so release candidates fail before publish when packaged trust assets disappear.
  artifacts:
    - test/package.test.js
  key_links:
    - npm pack dry-run includes data/proof/watchmaker-flow/README.md
    - npm pack dry-run includes the data/proof/voice-dna bundle
    - npm pack dry-run includes scriven-book.typst, scriven-epub.css, and scriven-academic.latex
---

<objective>
Extend Scriven's package dry-run tests so packaged proof artifacts and shipped export templates are enforced as release-time assets.

Purpose: the trust layer is only complete if the package artifact includes the proof and template files the launch surface tells users are bundled today.
Output: package dry-run assertions that protect proof bundles and shipped export-template assets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-trust-regression-coverage/16-CONTEXT.md
@.planning/phases/16-trust-regression-coverage/16-RESEARCH.md
@.planning/phases/16-trust-regression-coverage/16-01-PLAN.md
@package.json
@test/package.test.js
@docs/shipped-assets.md
@docs/proof-artifacts.md
@data/proof/watchmaker-flow/README.md
@data/proof/voice-dna/README.md
@data/proof/voice-dna/STYLE-GUIDE-EXCERPT.md
@data/proof/voice-dna/UNGUIDED-SAMPLE.md
@data/proof/voice-dna/GUIDED-SAMPLE.md
@data/export-templates/scriven-book.typst
@data/export-templates/scriven-epub.css
@data/export-templates/scriven-academic.latex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend package dry-run coverage for trust-critical shipped assets</name>
  <files>test/package.test.js</files>
  <action>Update `test/package.test.js` so the existing `npm pack --dry-run` coverage protects the new trust-critical packaged assets introduced in Phases 13-15. Add explicit assertions for the watchmaker proof bundle, the full Voice DNA proof bundle, and the three currently shipped export templates from `docs/shipped-assets.md`. Keep the assertions stable and path-based rather than relying on file counts or tarball ordering. Reuse the existing `packOutput` fixture so `npm pack --dry-run` still runs only once per test file.</action>
  <verify>
    <automated>node --test test/package.test.js</automated>
  </verify>
  <done>Package dry-run tests now fail if proof artifacts or currently shipped export templates drop out of the npm artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package files list -> release artifact | `package.json` `files` and copied asset directories determine what users actually receive from npm. |
| launch-surface promises -> shipped package contents | Proof bundles and export templates described as bundled must remain present in `npm pack --dry-run`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | T | `package.json`, package artifact | mitigate | Assert package dry-run contains the exact proof bundle and shipped export-template paths promised on the launch surface. |
| T-16-02-02 | R | `test/package.test.js` | mitigate | Reuse the existing pack fixture and use direct path assertions to keep the gate stable and readable. |
</threat_model>

<verification>
- `node --test test/package.test.js` passes
- `npm pack --dry-run` output includes proof bundles and shipped export templates
</verification>

<success_criteria>
- Package dry-run protects packaged proof artifacts and shipped export templates
- Release-time enforcement remains wired through `npm test` and `prepublishOnly`
</success_criteria>

<output>
After completion, create `.planning/phases/16-trust-regression-coverage/16-02-SUMMARY.md`
</output>
