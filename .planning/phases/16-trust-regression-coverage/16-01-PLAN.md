---
phase: 16-trust-regression-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase16-trust-regression.test.js
autonomous: true
requirements:
  - QA-01
user_setup: []
must_haves:
  truths:
    - A maintainer can run automated checks that fail if trust-critical launch files or proof artifacts disappear.
    - A maintainer can run automated checks that fail if canonical launch/proof/runtime links drift away from existing files.
    - A maintainer can run automated checks that fail if stale launch phrases reappear on the README surface.
    - A maintainer can run automated checks that fail if the runtime-support document drifts away from installer runtime labels.
  artifacts:
    - test/phase16-trust-regression.test.js
  key_links:
    - docs/proof-artifacts.md references canonical proof files that exist in data/proof/
    - README.md and docs/getting-started.md still route users to docs/proof-artifacts.md and docs/runtime-support.md
    - docs/shipped-assets.md still reflects the actual files in data/export-templates/
---

<objective>
Encode Scriven's trust-critical docs and assets into regression tests so launch-surface drift is caught automatically during `npm test`.

Purpose: the trust gains from Phases 13-15 are fragile unless the repo now fails fast when canonical files disappear, links go stale, or old overclaims return.
Output: a dedicated trust-regression test suite that validates canonical links, shipped-asset truth, runtime-support coverage, and forbidden launch regressions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-trust-regression-coverage/16-CONTEXT.md
@.planning/phases/16-trust-regression-coverage/16-RESEARCH.md
@README.md
@docs/getting-started.md
@docs/proof-artifacts.md
@docs/runtime-support.md
@docs/shipped-assets.md
@bin/install.js
@test/package.test.js
@test/installer.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trust-critical launch and proof regression tests</name>
  <files>test/phase16-trust-regression.test.js</files>
  <action>Create `test/phase16-trust-regression.test.js` as a focused Node test file for `QA-01`. Cover the canonical trust surfaces rather than scattered marketing copy. At minimum, assert that `docs/shipped-assets.md` matches the actual export-template files bundled in `data/export-templates/`, that files explicitly marked as not shipped are indeed absent, that trust-critical launch-file paths still exist, that canonical proof-hub artifacts referenced from `docs/proof-artifacts.md` exist, that `README.md` and `docs/getting-started.md` still link to `docs/proof-artifacts.md` and `docs/runtime-support.md`, that stale forbidden claims like `All features shipped` and `full support` do not reappear in `README.md`, and that `docs/runtime-support.md` still includes every installer runtime label exported by `bin/install.js`. Keep assertions deterministic and path-based; do not introduce brittle full-document snapshot tests.</action>
  <verify>
    <automated>node --test test/phase16-trust-regression.test.js</automated>
  </verify>
  <done>The repo now has a deterministic trust-regression test suite that protects canonical docs, links, and shipped-asset truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical docs -> user expectations | `README.md`, `docs/proof-artifacts.md`, `docs/runtime-support.md`, and `docs/shipped-assets.md` define what users believe the repo proves. |
| filesystem -> claimed shipped assets | `docs/shipped-assets.md` must stay aligned to what is actually present under `data/` and other launch-critical paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | T | `docs/shipped-assets.md` vs `data/export-templates/` | mitigate | Assert the canonical shipped-template inventory matches the filesystem exactly for current templates and planned-not-shipped files. |
| T-16-01-02 | R | `README.md`, `docs/getting-started.md` | mitigate | Assert proof/runtime routing links remain present and stale forbidden claims remain absent. |
| T-16-01-03 | R | `docs/proof-artifacts.md`, `docs/runtime-support.md` | mitigate | Assert canonical proof artifacts exist and runtime-support labels still cover the installer registry. |
</threat_model>

<verification>
- `node --test test/phase16-trust-regression.test.js` passes
- `npm test` passes with the new test file included
</verification>

<success_criteria>
- Trust-critical canonical docs and files are protected by automated tests
- Launch-surface overclaim regressions fail locally before publish
- Runtime-support and proof-hub docs are checked as live source-of-truth files rather than as aspirational docs
</success_criteria>

<output>
After completion, create `.planning/phases/16-trust-regression-coverage/16-01-SUMMARY.md`
</output>
