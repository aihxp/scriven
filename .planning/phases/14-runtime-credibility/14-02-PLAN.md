---
phase: 14-runtime-credibility
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - README.md
  - docs/getting-started.md
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - RUNTIME-03
user_setup: []
must_haves:
  truths:
    - A first-time visitor can find Scriven's runtime support framing from launch and onboarding docs without reading source code.
    - Users can distinguish installer targets, reference runtimes, and generic or best-effort paths from the wording in root docs.
    - Root instruction docs reinforce the same Node baseline and support-level language that the canonical matrix defines.
  artifacts:
    - README.md
    - docs/getting-started.md
    - AGENTS.md
    - CLAUDE.md
  key_links:
    - README.md links to docs/runtime-support.md and summarizes the same support-level vocabulary defined there.
    - docs/getting-started.md uses the same Node baseline and runtime-target framing as docs/runtime-support.md.
    - AGENTS.md and CLAUDE.md preserve the same Node floor and runtime-credibility posture so future planning does not reintroduce drift.
---

<objective>
Push the canonical runtime policy into launch, onboarding, and root instruction docs so users can understand Scriven's runtime confidence from the first read.

Purpose: A source-of-truth matrix is not enough if README, onboarding, or planning instructions still imply uniform runtime support or a different Node baseline.
Output: Launch and onboarding docs that link to the runtime matrix, use consistent support language, and clearly separate verified evidence from installer-target coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-runtime-credibility/14-01-SUMMARY.md
@docs/runtime-support.md
@README.md
@docs/getting-started.md
@AGENTS.md
@CLAUDE.md
@docs/shipped-assets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align README runtime claims to the canonical matrix</name>
  <files>README.md</files>
  <action>Update `README.md` so its runtime section reflects the credibility model from `docs/runtime-support.md` rather than implying uniform support across all listed runtimes. Keep the existing Phase 13 trust posture: describe the listed environments as installer targets, retain `Claude Code` as the primary reference runtime only if the wording stays conservative, and add a direct link to `docs/runtime-support.md` for the full compatibility matrix. Make the same Node 20+ baseline visible in the install or runtime section so the README no longer depends on `docs/getting-started.md` to communicate that requirement.</action>
  <verify>
    <automated>rg -n "Node\\.js 20\\+|docs/runtime-support\\.md|Installer Targets|reference runtime|compatibility matrix" README.md</automated>
    <automated>! rg -n "full support" README.md</automated>
  </verify>
  <done>The README gives readers an honest summary of runtime confidence and points them to the canonical matrix for details.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite onboarding language around runtime evidence instead of blanket support</name>
  <files>docs/getting-started.md</files>
  <action>Revise `docs/getting-started.md` so the prerequisites and install steps match the canonical runtime-support framing. Replace phrases like `supported runtimes` with language grounded in current proof, such as installer targets or current runtime paths. Keep the onboarding tone fast and simple, but add one clear pointer to `docs/runtime-support.md` so users know where to check install type, support level, and verification status before choosing a runtime. Preserve the Node 20+ prerequisite and the progressive-disclosure tone from Phase 10; do not turn the getting-started guide into a runtime deep dive.</action>
  <verify>
    <automated>rg -n "Node\\.js 20\\+|docs/runtime-support\\.md|installer targets" docs/getting-started.md</automated>
    <automated>! rg -n "supported runtimes" docs/getting-started.md</automated>
  </verify>
  <done>The getting-started guide stays concise while accurately framing runtime claims and pointing users to the matrix for detail.</done>
</task>

<task type="auto">
  <name>Task 3: Lock future planning docs to the same runtime credibility language</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <action>Update `AGENTS.md` and `CLAUDE.md` so their Node guidance changes from a recommendation to the actual project policy established in Plan 14-01. Replace wording that says to `consider bumping` the minimum with explicit wording that the supported installer baseline is `>=20.0.0` / `Node 20+`. Add a short runtime-credibility note near the installer/runtime guidance or constraints section stating that `docs/runtime-support.md` is the canonical runtime matrix and that installer targets are not interchangeable proof of host-runtime parity. Keep both files synchronized so future planning or implementation work inherits the same trust posture.</action>
  <verify>
    <automated>rg -n ">=20\\.0\\.0|Node 20\\+|docs/runtime-support\\.md|installer targets|host-runtime parity" AGENTS.md CLAUDE.md</automated>
    <automated>! rg -n "consider bumping" AGENTS.md CLAUDE.md</automated>
  </verify>
  <done>`AGENTS.md` and `CLAUDE.md` now reinforce the same runtime policy as the source-of-truth docs, reducing the chance of future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launch and onboarding docs -> user purchase or install expectations | Readers decide whether Scriven feels credible based on README and getting-started wording. |
| root instruction docs -> future planning and implementation work | Stale project instructions can reintroduce conflicting Node and runtime-support claims in later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | R | `README.md`, `docs/getting-started.md` | mitigate | Link both docs to `docs/runtime-support.md` and use the same `Node 20+`, installer-target, and verification-status terminology. |
| T-14-02-02 | T | `README.md`, `docs/getting-started.md` | mitigate | Remove blanket `supported runtimes` phrasing where it overstates current evidence, replacing it with matrix-backed wording. |
| T-14-02-03 | R | `AGENTS.md`, `CLAUDE.md` | mitigate | Convert Node 20 from recommendation to policy and mark `docs/runtime-support.md` as the canonical runtime credibility source for future plans. |
</threat_model>

<verification>
- `rg -n "docs/runtime-support\\.md|Node\\.js 20\\+" README.md docs/getting-started.md AGENTS.md CLAUDE.md` returns matches
- `rg -n "supported runtimes" docs/getting-started.md` returns no launch-overclaim wording after the rewrite
- `rg -n "consider bumping" AGENTS.md CLAUDE.md` returns no matches
- `npm test` still passes after the documentation updates
</verification>

<success_criteria>
- README and getting-started docs expose the runtime matrix and support-level framing without requiring source-code inspection
- Users can distinguish installer targets from stronger verification claims in launch and onboarding copy
- AGENTS.md and CLAUDE.md now preserve the same Node and runtime credibility policy as the canonical matrix
</success_criteria>

<output>
After completion, create `.planning/phases/14-runtime-credibility/14-02-SUMMARY.md`
</output>
