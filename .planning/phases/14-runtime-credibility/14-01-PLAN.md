---
phase: 14-runtime-credibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/install.js
  - docs/runtime-support.md
  - docs/architecture.md
  - docs/shipped-assets.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
  - .planning/research/SUMMARY.md
  - .planning/research/PITFALLS.md
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
user_setup: []
must_haves:
  truths:
    - A user sees one minimum supported Node.js baseline across package metadata and installer guidance.
    - A user can inspect one canonical runtime-support document that explains support levels and evidence without reading source code.
    - Maintainer-facing architecture docs point to the same runtime policy instead of restating conflicting support claims.
    - The trust-critical launch-file inventory records `docs/runtime-support.md` as a new canonical runtime-truth document.
    - Planning-facing docs that currently discuss Node or runtime support no longer preserve the obsolete Node 18 baseline as live project guidance.
  artifacts:
    - package.json
    - bin/install.js
    - docs/runtime-support.md
    - docs/architecture.md
    - docs/shipped-assets.md
    - .planning/PROJECT.md
    - .planning/codebase/STACK.md
    - .planning/research/SUMMARY.md
    - .planning/research/PITFALLS.md
  key_links:
    - package.json engines.node matches the Node baseline stated in docs/runtime-support.md.
    - docs/runtime-support.md derives its runtime rows from the keys and install paths defined in bin/install.js.
    - docs/architecture.md points readers to docs/runtime-support.md for support-level and verification-status truth.
    - docs/shipped-assets.md lists docs/runtime-support.md under trust-critical launch files.
    - .planning/PROJECT.md and runtime-related planning references use the same Node baseline as package.json and docs/runtime-support.md.
---

<objective>
Establish the canonical runtime policy surface for Scriven by choosing one Node.js floor and documenting what the repo actually proves for each installer target.

Purpose: Phase 14 fails immediately if package metadata, installer UX, and runtime policy docs disagree about the Node baseline or imply more runtime certainty than the repo can support.
Output: A new runtime-support source of truth plus aligned package metadata, installer messaging, and architecture guidance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-runtime-credibility/14-RESEARCH.md
@package.json
@bin/install.js
@docs/architecture.md
@test/installer.test.js
@docs/shipped-assets.md

<interfaces>
From package.json:
```json
"engines": { "node": ">=18.0.0" }
```

From bin/install.js:
```javascript
const RUNTIMES = {
  'claude-code': { label: 'Claude Code', type: 'commands', ... },
  'cursor': { label: 'Cursor', type: 'commands', ... },
  'gemini-cli': { label: 'Gemini CLI', type: 'commands', ... },
  'codex': { label: 'Codex CLI', type: 'commands', ... },
  'opencode': { label: 'OpenCode', type: 'commands', ... },
  'copilot': { label: 'GitHub Copilot', type: 'commands', ... },
  'windsurf': { label: 'Windsurf', type: 'commands', ... },
  'antigravity': { label: 'Antigravity', type: 'commands', ... },
  'manus': { label: 'Manus Desktop', type: 'skills', ... },
  'generic': { label: 'Generic (SKILL.md)', type: 'skills', ... },
};
```

From test/installer.test.js:
```javascript
assert.ok('claude-code' in RUNTIMES, 'missing claude-code runtime');
assert.ok('cursor' in RUNTIMES, 'missing cursor runtime');
...
assert.ok('manus' in RUNTIMES, 'missing manus runtime');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical runtime policy and compatibility matrix</name>
  <files>docs/runtime-support.md</files>
  <action>Create `docs/runtime-support.md` as the single source of truth for runtime credibility per RUNTIME-01 and RUNTIME-02. Add these sections in order: `# Runtime Support`, `## Node.js Baseline`, `## Evidence Levels`, `## Support Levels`, `## Verification Status`, `## Runtime Compatibility Matrix`, and `## What Scriven Proves Today`. In the matrix, use explicit columns for runtime, install type, install path shape, repo evidence, support level, and verification status. Set one explicit installer baseline of `Node.js 20+` / `>=20.0.0` and state that Node is required for the installer and tests, not as a runtime dependency for the markdown command system. Build the runtime matrix directly from `RUNTIMES` in `bin/install.js`; include every current runtime key and show install type, installation path shape, repo evidence, support level, and verification status. Keep support language conservative: distinguish a primary reference runtime, standard installer targets, and generic or skill-file fallbacks, but do not invent host-runtime verification the repo cannot prove. Explicitly say installer-path coverage and registry tests are not the same as verified end-to-end runtime parity.</action>
  <verify>
    <automated>rg -n "Node\\.js 20\\+|>=20\\.0\\.0|Runtime Compatibility Matrix|Support Levels|Verification Status" docs/runtime-support.md</automated>
    <automated>rg -n "^\\| Runtime \\| Install Type \\| Install Path Shape \\| Repo Evidence \\| Support Level \\| Verification Status \\|$" docs/runtime-support.md</automated>
    <automated>for label in "Claude Code" "Cursor" "Gemini CLI" "Codex CLI" "OpenCode" "GitHub Copilot" "Windsurf" "Antigravity" "Manus Desktop" "Generic \\(SKILL\\.md\\)"; do rg -n "^\\| ${label} \\| .* \\| .* \\| .* \\| .* \\| .* \\|$" docs/runtime-support.md >/dev/null || exit 1; done</automated>
  </verify>
  <done>`docs/runtime-support.md` exists as the canonical runtime policy, includes all current installer targets, and defines evidence/support language that matches present repo proof.</done>
</task>

<task type="auto">
  <name>Task 2: Align package metadata and installer guidance to the chosen Node baseline</name>
  <files>package.json, bin/install.js</files>
  <action>Update `package.json` so `engines.node` matches the Node floor defined in `docs/runtime-support.md` exactly, using `>=20.0.0` rather than the current Node 18 baseline. Then update `bin/install.js` so the installer banner or early guidance tells users that Scriven's installer requires Node 20+ and that listed runtimes are installer targets with varying support evidence. Keep this messaging brief and honest: the installer should not imply that every runtime is equivalently verified, and it should not assume `docs/` is locally installed from npm. Prefer wording that points users to the repository README/runtime-support documentation for the full matrix while keeping the terminal guidance self-contained.</action>
  <verify>
    <automated>rg -n "\"node\": \">=20\\.0\\.0\"" package.json</automated>
    <automated>rg -n "Node\\.js 20\\+|installer targets with varying support evidence" bin/install.js</automated>
  </verify>
  <done>Package metadata and installer UX present the same Node baseline and a credibility-aware explanation of runtime claims.</done>
</task>

<task type="auto">
  <name>Task 3: Align architecture, trust inventory, and planning docs to the canonical runtime policy</name>
  <files>docs/architecture.md, docs/shipped-assets.md, .planning/PROJECT.md, .planning/codebase/STACK.md, .planning/research/SUMMARY.md, .planning/research/PITFALLS.md</files>
  <action>Revise the installer/runtime sections of `docs/architecture.md` so they preserve the factual detection and install-strategy details already documented, but stop short of implying uniform runtime support. Add a short runtime-credibility paragraph that points readers to `docs/runtime-support.md` for the authoritative Node baseline, support levels, and verification status. Update `docs/shipped-assets.md` so its trust-critical launch-file inventory includes `docs/runtime-support.md` as the canonical runtime-truth document added in this phase. Then update planning-facing documents that still function as active project guidance so they no longer preserve stale Node 18 guidance: at minimum `.planning/PROJECT.md`, `.planning/codebase/STACK.md`, `.planning/research/SUMMARY.md`, and `.planning/research/PITFALLS.md` should either state the same `Node 20+` / `>=20.0.0` baseline or explicitly frame older statements as historical context rather than current policy. Do not create a second runtime matrix in planning docs; defer support-level and verification truth to `docs/runtime-support.md`.</action>
  <verify>
    <automated>rg -n "docs/runtime-support\\.md|Node\\.js 20\\+|support levels|verification status" docs/architecture.md</automated>
    <automated>rg -n "docs/runtime-support\\.md" docs/shipped-assets.md</automated>
    <automated>rg -n ">=20\\.0\\.0|Node 20\\+" .planning/PROJECT.md .planning/codebase/STACK.md .planning/research/SUMMARY.md .planning/research/PITFALLS.md</automated>
    <automated>! rg -n ">=18\\.0\\.0|Node 18" .planning/codebase/STACK.md .planning/research/SUMMARY.md .planning/research/PITFALLS.md</automated>
  </verify>
  <done>`docs/architecture.md` still explains installer mechanics, and planning-facing docs no longer contradict the chosen Node baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata and installer output -> user expectations | Users infer baseline support and installation confidence from `package.json` and terminal copy. |
| runtime registry -> runtime policy docs | Unsupported or stale matrix rows would create claim-vs-surface drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | T | `package.json`, `bin/install.js`, `docs/runtime-support.md` | mitigate | Use one explicit Node floor (`>=20.0.0`) and require every user-facing baseline mention to match it verbatim. |
| T-14-01-02 | R | `docs/runtime-support.md` | mitigate | Derive matrix rows from `RUNTIMES` and `test/installer.test.js`, and explicitly separate installer-path evidence from host-runtime verification. |
| T-14-01-03 | I | `docs/architecture.md` | mitigate | Keep mechanics in architecture docs, but centralize support-level and verification claims in `docs/runtime-support.md` to prevent conflicting interpretations. |
</threat_model>

<verification>
- `rg -n "\"node\": \">=20\\.0\\.0\"" package.json` returns a match
- `rg -n "Node\\.js 20\\+|Runtime Compatibility Matrix|Verification Status" docs/runtime-support.md` returns matches
- `rg -n "^\\| Runtime \\| Install Type \\| Install Path Shape \\| Repo Evidence \\| Support Level \\| Verification Status \\|$" docs/runtime-support.md` returns a match
- `for label in "Claude Code" "Cursor" "Gemini CLI" "Codex CLI" "OpenCode" "GitHub Copilot" "Windsurf" "Antigravity" "Manus Desktop" "Generic \\(SKILL\\.md\\)"; do rg -n "^\\| ${label} \\| .* \\| .* \\| .* \\| .* \\| .* \\|$" docs/runtime-support.md >/dev/null || exit 1; done` succeeds
- `rg -n "docs/runtime-support\\.md" docs/architecture.md` returns a match
- `rg -n "docs/runtime-support\\.md" docs/shipped-assets.md` returns a match
- `rg -n ">=20\\.0\\.0|Node 20\\+" .planning/PROJECT.md .planning/codebase/STACK.md .planning/research/SUMMARY.md .planning/research/PITFALLS.md` returns matches
- `npm test` passes after the metadata and installer copy changes
</verification>

<success_criteria>
- `package.json`, `bin/install.js`, and `docs/runtime-support.md` all state the same Node baseline
- Scriven has one canonical runtime-support document that lists each installer target, install path shape, support level, and verification status
- `docs/architecture.md` and planning-facing docs no longer act as conflicting sources of truth for runtime policy
</success_criteria>

<output>
After completion, create `.planning/phases/14-runtime-credibility/14-01-SUMMARY.md`
</output>
