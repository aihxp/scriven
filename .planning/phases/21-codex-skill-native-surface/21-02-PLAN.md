---
phase: 21-codex-skill-native-surface
plan: "02"
type: execute
wave: 1
depends_on:
  - "21-01"
files_modified:
  - bin/install.js
  - test/installer.test.js
requirements:
  - RUNTIME-09
  - RUNTIME-11
autonomous: true
user_setup: []
---

<objective>
Wire the Codex runtime into the installer as a skill-native target while preserving mirrored command and agent compatibility assets.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Register Codex as a skill-native runtime with mirror paths</name>
  <files>bin/install.js, test/installer.test.js</files>
  <action>Keep Codex typed as `skills`, define skill plus mirror output paths, and verify the installer writes the expected Codex-owned assets.</action>
  <verify>
    <automated>node --test test/installer.test.js</automated>
  </verify>
  <done>Codex install output is represented truthfully in the runtime registry and test suite.</done>
</task>

</tasks>

<success_criteria>
- Codex installs generate skills while retaining mirrored command markdown and agent paths
</success_criteria>
