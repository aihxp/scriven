---
phase: 21-codex-skill-native-surface
plan: "03"
type: execute
wave: 1
depends_on:
  - "21-01"
  - "21-02"
files_modified:
  - bin/install.js
  - test/installer.test.js
requirements:
  - RUNTIME-09
autonomous: true
user_setup: []
---

<objective>
Translate Codex-facing command references to the `$scr-*` surface inside generated wrapper content.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Generate Codex-facing wrapper copy</name>
  <files>bin/install.js, test/installer.test.js</files>
  <action>Ensure generated skill wrappers refer Codex users to `$scr-*` invocations while still pointing at the mirrored installed command files for execution truth.</action>
  <verify>
    <automated>node --test test/installer.test.js</automated>
  </verify>
  <done>Codex wrappers guide users through the native `$` surface instead of `/scr:*` syntax.</done>
</task>

</tasks>

<success_criteria>
- Generated wrapper text consistently translates `/scr:*` references into `$scr-*`
</success_criteria>
