---
phase: 20-silent-multi-runtime-installer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - test/installer.test.js
requirements:
  - RUNTIME-08
  - RUNTIME-10
  - RUNTIME-11
autonomous: true
user_setup: []
must_haves:
  truths:
    - The installer supports a non-interactive CLI surface for runtime selection, install scope, mode selection, help, and version output.
    - One install run can target multiple runtimes without prompting and without silently falling back on invalid runtime input.
    - Reinstall behavior only removes Scriven-owned output paths, preserving unrelated user files outside those owned subtrees.
  artifacts:
    - bin/install.js
    - test/installer.test.js
  key_links:
    - Runtime selection, scope, and mode flags are parsed in the installer entrypoint rather than through interactive readline prompts.
    - Clean install helpers only replace Scriven-owned command mirrors and shipped agent files.
    - Installer tests fail if the non-interactive argument contract drifts.
---

<objective>
Add the silent multi-runtime installer surface for Scriven so users can script installs for Codex, Claude Code, and other supported runtimes without hanging on prompts.

Purpose: installation should feel reliable in shells, scripts, and CI-like flows while preserving Scriven's narrow trust model and zero-runtime-dependency architecture.
Output: updated installer argument parsing and install orchestration, plus tests that protect the non-interactive install contract and clean reinstall semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-silent-multi-runtime-installer/20-CONTEXT.md
@bin/install.js
@test/installer.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-interactive installer argument parsing</name>
  <files>bin/install.js</files>
  <action>Refactor the installer so it can parse runtime, scope, mode, help, and version flags without entering interactive readline mode. Support explicit runtime selection, detected-runtime installation, and a silent completion path that prints compact output. Reject unknown runtimes with a clear error instead of defaulting silently.</action>
  <verify>
    <automated>node --check bin/install.js</automated>
  </verify>
  <done>The installer can be run non-interactively with the documented flags and does not hang waiting for terminal input.</done>
</task>

<task type="auto">
  <name>Task 2: Add clean install helpers and cover them with tests</name>
  <files>bin/install.js, test/installer.test.js</files>
  <action>Introduce clean install semantics for Scriven-owned output paths. Command mirrors may be replaced wholesale when Scriven owns the subtree, while agent directories should remove only the files that correspond to Scriven's shipped agent set before copying fresh versions. Extend installer tests to cover argument parsing, runtime grouping, and the new Codex/Claude non-interactive install contract.</action>
  <verify>
    <automated>node --test test/installer.test.js</automated>
  </verify>
  <done>Silent install behavior and clean reinstall rules are protected by automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> install behavior | The argument surface controls where Scriven writes files and whether installs block waiting for prompts. |
| clean install -> user files | Reinstall logic must only replace Scriven-owned outputs, not unrelated runtime files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | T | `bin/install.js` | mitigate | Fail fast on invalid runtime keys and keep the flag surface explicit. |
| T-20-01-02 | D | `bin/install.js` | mitigate | Skip readline entirely in non-interactive mode so installs cannot hang on hidden prompts. |
| T-20-01-03 | T | runtime output paths | mitigate | Remove only Scriven-owned command mirrors and shipped agent files before copying fresh versions. |
</threat_model>

<verification>
- `node --check bin/install.js` passes
- `node --test test/installer.test.js` passes
</verification>

<success_criteria>
- Silent installer flags work without prompts
- Multi-runtime installs are supported in one run
- Clean reinstall semantics protect unrelated user files
</success_criteria>

<output>
After completion, create `.planning/phases/20-silent-multi-runtime-installer/20-01-SUMMARY.md`
</output>
