---
phase: 20-silent-multi-runtime-installer
plan: "02"
type: execute
wave: 1
depends_on:
  - "20-01"
files_modified:
  - bin/install.js
  - test/installer.test.js
requirements:
  - RUNTIME-10
  - RUNTIME-11
autonomous: true
user_setup: []
must_haves:
  truths:
    - One installer run can target multiple runtimes without re-entering interactive prompts between installs.
    - Shared install metadata is written once per run and records the full runtime list that was installed.
    - Reinstall behavior only removes Scriven-owned command mirrors and shipped agent files, preserving unrelated host files.
  artifacts:
    - bin/install.js
    - test/installer.test.js
  key_links:
    - Multi-runtime selection and run summary stay inside the installer orchestration path.
    - Shared `.scriven/settings.json` output captures all selected runtimes from a single install run.
    - Clean install helpers only replace Scriven-owned command and agent outputs.
---

<objective>
Finish the silent installer flow by covering the multi-runtime orchestration and clean reinstall behavior that sit on top of the new CLI flag surface.

Purpose: silent installs should feel dependable in scripts and real host directories, not just at the argument-parser layer.
Output: installer logic that writes shared settings once, installs all requested runtimes in one pass, and only replaces Scriven-owned runtime outputs during reinstalls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-silent-multi-runtime-installer/20-CONTEXT.md
@bin/install.js
@test/installer.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete multi-runtime silent install orchestration</name>
  <files>bin/install.js</files>
  <action>Ensure one silent installer run can install to multiple requested runtimes, writes shared `.scriven` data once, and emits a compact completion line instead of per-runtime prompt choreography.</action>
  <verify>
    <automated>node bin/install.js --help</automated>
  </verify>
  <done>Silent install behavior works end-to-end for a multi-runtime request without dropping back into interactive flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add clean reinstall coverage for Scriven-owned outputs</name>
  <files>bin/install.js, test/installer.test.js</files>
  <action>Keep the clean install helpers narrow so command mirrors can be replaced wholesale when Scriven owns the subtree, while agent directories remove only Scriven-shipped files before fresh copies land. Extend tests where needed so the reinstall contract stays enforced.</action>
  <verify>
    <automated>node --test test/installer.test.js</automated>
  </verify>
  <done>Silent install behavior and clean reinstall rules are protected by automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime list -> install orchestration | One non-interactive request can fan out into multiple runtime writes in a single run. |
| clean install -> user files | Reinstall logic must only replace Scriven-owned outputs, not unrelated files in host runtime directories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | D | multi-runtime install path | mitigate | Reuse parsed runtime selections and avoid returning to readline once non-interactive mode is active. |
| T-20-02-02 | T | command/agent output paths | mitigate | Clean only Scriven-owned output subtrees or shipped agent files before copying fresh assets. |
| T-20-02-03 | R | silent install contract | mitigate | Cover multi-runtime orchestration and reinstall semantics with installer tests and CLI smoke checks. |
</threat_model>

<verification>
- `node bin/install.js --help` passes
- `node --test test/installer.test.js` passes
</verification>

<success_criteria>
- Silent multi-runtime installs complete without prompts
- Shared install metadata reflects the runtimes selected in one run
- Clean reinstall semantics protect unrelated user files
</success_criteria>

<output>
After completion, create `.planning/phases/20-silent-multi-runtime-installer/20-02-SUMMARY.md`
</output>
