---
phase: 05-export-publishing
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - test/phase5-export-publishing.test.js
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05, EXP-06, EXP-07, EXP-08, EXP-09, EXP-10, EXP-11, EXP-12, EXP-13, EXP-14, EXP-15, EXP-16, EXP-17]
must_haves:
  truths:
    - "Test suite validates all 17 EXP requirements via file existence and content checks"
    - "Tests follow established pattern: node:test framework, describe blocks per requirement ID"
    - "All tests pass with npm test"
  artifacts:
    - path: "test/phase5-export-publishing.test.js"
      provides: "Phase 5 test suite covering EXP-01 through EXP-17"
      contains: "EXP-01|EXP-17"
  key_links:
    - from: "test/phase5-export-publishing.test.js"
      to: "commands/scr/export.md"
      via: "validates export command content"
      pattern: "export\\.md"
    - from: "test/phase5-export-publishing.test.js"
      to: "commands/scr/publish.md"
      via: "validates publish wizard content"
      pattern: "publish\\.md"
    - from: "test/phase5-export-publishing.test.js"
      to: "data/export-templates/"
      via: "validates template files exist"
      pattern: "export-templates"
---

<objective>
Create the Phase 5 test suite validating all 17 EXP requirements.

Purpose: Tests verify that all export commands, templates, and CONSTRAINTS.json entries exist with correct content. Follows the established test pattern from prior phases (node:test framework, describe blocks per requirement ID with decision traceability).

Output: 1 test file covering all 17 requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-publishing/05-CONTEXT.md

@test/phase4-quality-publishing.test.js
@commands/scr/export.md
@commands/scr/publish.md
@commands/scr/autopilot-publish.md
@commands/scr/manuscript-stats.md
@data/CONSTRAINTS.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5 test suite</name>
  <files>test/phase5-export-publishing.test.js</files>
  <read_first>
    - test/phase4-quality-publishing.test.js (established test pattern -- describe blocks per requirement, file existence + content checks, decision ID traceability)
    - test/phase3-creative-toolkit.test.js (additional test pattern reference)
    - commands/scr/export.md (verify content to test against)
    - commands/scr/publish.md (verify content to test against)
    - commands/scr/autopilot-publish.md (verify content to test against)
    - commands/scr/manuscript-stats.md (verify content to test against)
    - data/CONSTRAINTS.json (verify export and command entries)
    - data/export-templates/scriven-book.typst (verify exists)
    - data/export-templates/scriven-epub.css (verify exists)
    - data/export-templates/scriven-academic.latex (verify exists)
  </read_first>
  <acceptance_criteria>
    - test/phase5-export-publishing.test.js exists
    - Contains describe blocks for all 17 requirements (EXP-01 through EXP-17)
    - Decision IDs (D-01 through D-09) referenced in relevant describe block names
    - Tests validate file existence for: export.md, publish.md, autopilot-publish.md, manuscript-stats.md
    - Tests validate template existence for: scriven-book.typst, scriven-epub.css, scriven-academic.latex
    - Tests validate export.md content: OUTLINE assembly, command -v pandoc, --reference-doc, --pdf-engine=typst, --epub-cover-image, fountain, screenplain, citeproc, kdp-package, 0.002252, ingram-package, gs -dPDFX, query-package, submission-package
    - Tests validate publish.md content: kdp-paperback, kdp-ebook, query-submission, ebook-wide, prerequisite/checklist
    - Tests validate autopilot-publish.md content: voice-check, continuity-check, warn, preset
    - Tests validate manuscript-stats.md content: word count, page count, reading time
    - Tests validate CONSTRAINTS.json has export and autopilot-publish entries
    - Running `node --test test/phase5-export-publishing.test.js` passes all tests
  </acceptance_criteria>
  <action>
Create test file following the established Phase 3/4 test pattern: node:test framework, describe blocks named with requirement ID and decision ID for traceability.

**Structure:**

```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const commandsDir = path.join(ROOT, 'commands', 'scr');
const templatesDir = path.join(ROOT, 'data', 'export-templates');
const constraintsPath = path.join(ROOT, 'data', 'CONSTRAINTS.json');
```

**Test groups by requirement:**

1. **Export templates** (supports D-06):
   - scriven-book.typst exists, contains margin/page size params
   - scriven-epub.css exists, contains font-family/line-height
   - scriven-academic.latex exists, contains documentclass

2. **EXP-01: export markdown (D-01, D-03)** -- export.md exists, has YAML frontmatter, contains markdown format section, references OUTLINE for assembly
3. **EXP-02: export docx (D-02)** -- contains docx section, --reference-doc=scriven-manuscript.docx, command -v pandoc
4. **EXP-03: export docx --formatted** -- contains formatted section, --reference-doc=scriven-formatted.docx
5. **EXP-04: export pdf (D-01)** -- contains pdf section, --pdf-engine=typst
6. **EXP-05: export pdf --print-ready** -- contains print-ready section, --template=scriven-book.typst, paper dimensions
7. **EXP-06: export epub** -- contains epub section, --epub-cover-image, --css=scriven-epub.css
8. **EXP-07: export fountain** -- contains fountain section, Fountain format markers (INT./EXT.)
9. **EXP-08: export fdx** -- contains fdx section, references screenplain
10. **EXP-09: export latex** -- contains latex section, --template=scriven-academic.latex, --citeproc
11. **EXP-10: export kdp-package (D-05)** -- contains kdp-package section, spine width formula with 0.002252
12. **EXP-11: export ingram-package** -- contains ingram-package section, gs -dPDFX, CMYK
13. **EXP-12: export query-package** -- contains query-package section
14. **EXP-13: export submission-package** -- contains submission-package section
15. **EXP-14: publish wizard (D-07)** -- publish.md exists, contains prerequisite checking, shows checklist
16. **EXP-15: publish presets (D-08)** -- contains kdp-paperback, kdp-ebook, query-submission, ebook-wide
17. **EXP-16: autopilot-publish (D-09)** -- autopilot-publish.md exists, contains voice-check, continuity-check, warn
18. **EXP-17: manuscript-stats** -- manuscript-stats.md exists, contains word count, page count, reading time

19. **CONSTRAINTS.json entries** -- export command entry exists with category publishing, autopilot-publish entry exists

Each test uses the same pattern as Phase 4:
- `it('file.md exists', () => assert.ok(fs.existsSync(filePath)))`
- `it('has YAML frontmatter', () => { const fm = content.match(/^---\n([\s\S]*?)\n---/); assert.ok(fm); })`
- `it('contains X', () => assert.match(content, /pattern/))`
  </action>
  <verify>
    <automated>node --test test/phase5-export-publishing.test.js</automated>
  </verify>
  <done>All tests pass. Every EXP requirement (01-17) and every locked decision (D-01 through D-09) has test coverage. Test follows established Phase 3/4 pattern.</done>
</task>

</tasks>

<verification>
- `node --test test/phase5-export-publishing.test.js` passes all tests
- `npm test` passes (full suite including Phase 5)
- All 17 EXP requirements covered
- All 9 locked decisions (D-01 through D-09) traceable in test names
</verification>

<success_criteria>
- Test file covers EXP-01 through EXP-17
- Decision IDs D-01 through D-09 appear in describe block names
- All tests pass
- Full test suite (npm test) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-publishing/05-05-SUMMARY.md`
</output>
