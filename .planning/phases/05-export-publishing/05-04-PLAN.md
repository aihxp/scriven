---
phase: 05-export-publishing
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - commands/scr/publish.md
  - commands/scr/autopilot-publish.md
autonomous: true
requirements: [EXP-14, EXP-15, EXP-16]
must_haves:
  truths:
    - "publish.md checks prerequisites (front-matter, back-matter, blurb, synopsis, cover) and shows checklist with commands for missing pieces"
    - "publish.md supports 4 presets: kdp-paperback, kdp-ebook, query-submission, ebook-wide"
    - "autopilot-publish.md runs voice-check + continuity-check as quality gate before export pipeline"
  artifacts:
    - path: "commands/scr/publish.md"
      provides: "Publishing wizard with prerequisite checking and preset pipelines"
      contains: "kdp-paperback|kdp-ebook|query-submission|ebook-wide"
    - path: "commands/scr/autopilot-publish.md"
      provides: "Unattended publishing pipeline with quality gate"
      contains: "voice-check|continuity-check|quality.gate"
  key_links:
    - from: "commands/scr/publish.md"
      to: "commands/scr/export.md"
      via: "chains export commands based on preset/destination"
      pattern: "export"
    - from: "commands/scr/autopilot-publish.md"
      to: "commands/scr/voice-check.md"
      via: "runs voice-check as quality gate before pipeline"
      pattern: "voice.check"
    - from: "commands/scr/autopilot-publish.md"
      to: "commands/scr/continuity-check.md"
      via: "runs continuity-check as quality gate before pipeline"
      pattern: "continuity.check"
---

<objective>
Rewrite the publish wizard with prerequisite checking and presets, and create the autopilot-publish command for unattended pipeline execution.

Purpose: The publish wizard is the high-level orchestrator that chains export commands based on destination. Writers use presets for common scenarios (KDP, ebook, query) or the interactive wizard for guided publishing. Autopilot-publish adds a quality gate (voice-check + continuity-check) before running the full pipeline unattended.

Output: 1 rewritten command file (publish.md) + 1 new command file (autopilot-publish.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-publishing/05-CONTEXT.md
@.planning/phases/05-export-publishing/05-RESEARCH.md
@.planning/phases/05-export-publishing/05-01-SUMMARY.md
@.planning/phases/05-export-publishing/05-02-SUMMARY.md

@commands/scr/publish.md
@commands/scr/export.md
@commands/scr/voice-check.md
@commands/scr/continuity-check.md
@commands/scr/autopilot.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite publish wizard with presets and prerequisite checking</name>
  <files>commands/scr/publish.md</files>
  <read_first>
    - commands/scr/publish.md (existing shell -- understand current structure, keep YAML frontmatter pattern)
    - commands/scr/export.md (the export command this wizard chains)
    - commands/scr/polish.md (similar meta-command pattern that chains multiple sub-commands)
    - .planning/phases/05-export-publishing/05-CONTEXT.md (D-07 prerequisite checking, D-08 four presets)
    - .planning/phases/05-export-publishing/05-RESEARCH.md (Pattern 4: Publishing Wizard Flow, prerequisite checker logic)
    - data/CONSTRAINTS.json (publish entry, publishing_prerequisites section)
  </read_first>
  <acceptance_criteria>
    - publish.md has YAML frontmatter with description and argument-hint including --preset
    - Contains prerequisite checking per D-07: checks for front-matter, back-matter, blurb, synopsis, cover
    - Shows checklist with one-click /scr: commands for each missing prerequisite
    - Contains 4 preset definitions per D-08: kdp-paperback, kdp-ebook, query-submission, ebook-wide
    - kdp-paperback preset chains: front-matter -> back-matter -> export pdf --print-ready -> export kdp-package
    - kdp-ebook preset chains: front-matter -> back-matter -> export epub
    - query-submission preset chains: blurb -> synopsis -> query-letter -> export query-package
    - ebook-wide preset chains: front-matter -> back-matter -> export epub -> export pdf
    - Contains interactive wizard flow for when no preset is given
    - References CONSTRAINTS.json for work type availability
  </acceptance_criteria>
  <action>
Rewrite publish.md (the existing shell from Phase 1). Keep YAML frontmatter format but update content completely per D-07 and D-08.

**YAML frontmatter:**
- description: "Publishing wizard or preset-driven pipeline. Chains export commands based on destination."
- argument-hint: "[--preset <preset>] [--all]"

**STEP 1: LOAD CONTEXT**
- Load config.json for work type, title, author
- Load CONSTRAINTS.json for publishing_prerequisites and export availability

**STEP 2: ROUTE** (preset vs wizard)
- If --preset is given, jump to STEP 4 (preset pipeline)
- If no preset, run interactive wizard (STEP 3)

**STEP 3: INTERACTIVE WIZARD** (per D-07)
Check prerequisites and show checklist:
```
Publishing Readiness Checklist
==============================
[x] Complete draft (all units drafted)
[ ] Front matter -- run /scr:front-matter
[x] Back matter
[ ] Blurb -- run /scr:blurb
[ ] Synopsis -- run /scr:synopsis
[x] Cover art

Missing 2 prerequisites. Generate them now? (yes/no)
```

For each prerequisite, check:
- Complete draft: all OUTLINE.md units have corresponding draft files
- Front matter: .manuscript/front-matter/ has files
- Back matter: .manuscript/back-matter/ has files
- Blurb: .manuscript/output/blurb.md exists
- Synopsis: .manuscript/output/synopsis.md exists
- Cover art: .manuscript/output/cover.* exists

If writer says yes, run missing commands. Then ask destination:
"Where are you publishing? Options: kdp-paperback, kdp-ebook, ebook-wide, query-submission, ingram-paperback, or custom"

Map answer to preset pipeline.

**STEP 4: PRESET PIPELINES** (per D-08)
Four presets, each chains specific commands:

| Preset | Pipeline |
|--------|----------|
| kdp-paperback | /scr:front-matter (if missing) -> /scr:back-matter (if missing) -> /scr:export --format pdf --print-ready -> /scr:export --format kdp-package |
| kdp-ebook | /scr:front-matter (if missing) -> /scr:back-matter (if missing) -> /scr:export --format epub |
| query-submission | /scr:blurb (if missing) -> /scr:synopsis (if missing) -> /scr:query-letter (if missing) -> /scr:export --format query-package |
| ebook-wide | /scr:front-matter (if missing) -> /scr:back-matter (if missing) -> /scr:export --format epub -> /scr:export --format pdf |

For each step: check if output already exists, skip if so (tell writer), run if missing.

**STEP 5: REPORT**
Show summary of what was generated:
```
Publishing Complete
==================
Generated:
  - manuscript-print.pdf (interior, 6x9, 312 pages)
  - kdp-package/ (cover specs, metadata)

Next Steps:
  1. Upload interior PDF to KDP
  2. Create cover using dimensions in kdp-package/cover-specs.md
  3. Upload cover to KDP
```

NOTE: The existing publish.md has additional presets (ingram-paperback, academic-submission, thesis-defense, screenplay-query). Keep these as additional presets beyond the 4 locked presets. They use the same pattern -- chain the appropriate export commands.
  </action>
  <verify>
    <automated>grep -c "kdp-paperback" commands/scr/publish.md && grep -c "kdp-ebook" commands/scr/publish.md && grep -c "query-submission" commands/scr/publish.md && grep -c "ebook-wide" commands/scr/publish.md && grep -c "front-matter" commands/scr/publish.md && grep -c "prerequisite\|Prerequisite\|checklist\|Checklist" commands/scr/publish.md</automated>
  </verify>
  <done>publish.md has prerequisite checker showing missing items with /scr: commands. Four presets (kdp-paperback, kdp-ebook, query-submission, ebook-wide) chain the correct export commands. Interactive wizard guides first-time publishers.</done>
</task>

<task type="auto">
  <name>Task 2: Create autopilot-publish command</name>
  <files>commands/scr/autopilot-publish.md</files>
  <read_first>
    - commands/scr/autopilot.md (established autopilot pattern -- quality gates, unattended operation)
    - commands/scr/publish.md (the wizard this builds on -- understand preset pipeline structure)
    - commands/scr/voice-check.md (quality gate command)
    - commands/scr/continuity-check.md (quality gate command)
    - .planning/phases/05-export-publishing/05-CONTEXT.md (D-09 quality gate warns but doesn't block)
  </read_first>
  <acceptance_criteria>
    - commands/scr/autopilot-publish.md exists with YAML frontmatter containing description
    - Contains voice-check invocation as quality gate step
    - Contains continuity-check invocation as quality gate step
    - Contains "warn" language per D-09 (warns but doesn't block on quality gate failures)
    - Contains full pipeline: front-matter -> back-matter -> compile -> export -> package
    - References --preset flag for destination selection
    - Contains progress reporting during pipeline execution
  </acceptance_criteria>
  <action>
Create autopilot-publish command for unattended publishing pipeline execution per D-09.

**YAML frontmatter:**
- description: "Run full publishing pipeline unattended with quality gate (voice-check + continuity-check)."
- argument-hint: "--preset <preset>"

**STEP 1: LOAD CONTEXT**
- Load config.json for work type, title, author
- Load CONSTRAINTS.json for prerequisites
- Require --preset argument (no interactive mode in autopilot)
- Validate preset name against known presets

**STEP 2: QUALITY GATE** (per D-09)
Run quality checks before export pipeline:
1. Run /scr:voice-check -- check for voice drift
2. Run /scr:continuity-check -- check for continuity errors

**Quality gate policy (per D-09): warn but don't block.**
- If voice-check finds issues: log warnings, show summary, continue
- If continuity-check finds issues: log warnings, show summary, continue
- Show: "Quality gate: X voice warnings, Y continuity warnings. Proceeding with export."
- The writer can review the full reports after export completes

If STYLE-GUIDE.md does not exist (voice-check prerequisite), skip voice-check with a note.

**STEP 3: GENERATE PREREQUISITES**
Auto-generate any missing prerequisites for the chosen preset:
- front-matter (if missing and preset needs it)
- back-matter (if missing and preset needs it)
- blurb (if missing and preset needs it)
- synopsis (if missing and preset needs it)
- query-letter (if missing and preset needs it)

**STEP 4: RUN PRESET PIPELINE**
Execute the preset pipeline from publish.md:
- Show progress: "Step 1/4: Generating front matter..."
- Run each step
- Show progress: "Step 2/4: Generating back matter..."
- Continue through all steps

**STEP 5: FINAL REPORT**
Show complete summary:
```
Autopilot Publish Complete
==========================
Preset: kdp-paperback
Quality Gate: 2 voice warnings, 0 continuity warnings

Generated:
  - front-matter (7 elements)
  - back-matter (5 elements)
  - manuscript-print.pdf (312 pages)
  - kdp-package/ (cover specs + metadata)

Quality Reports:
  - Voice check: .manuscript/output/voice-check-report.md
  - Continuity check: .manuscript/output/continuity-check-report.md

Next Steps:
  1. Review quality warnings
  2. Upload files to KDP
```
  </action>
  <verify>
    <automated>test -f commands/scr/autopilot-publish.md && grep -c "voice-check" commands/scr/autopilot-publish.md && grep -c "continuity-check" commands/scr/autopilot-publish.md && grep -c "warn" commands/scr/autopilot-publish.md && grep -c "preset" commands/scr/autopilot-publish.md</automated>
  </verify>
  <done>autopilot-publish.md runs voice-check + continuity-check as quality gate (warns but doesn't block per D-09), auto-generates missing prerequisites, runs preset pipeline, and reports results.</done>
</task>

</tasks>

<verification>
- publish.md has prerequisite checker and 4 presets
- autopilot-publish.md runs quality gate before pipeline
- Both commands chain /scr:export with correct format flags
- Quality gate warns but doesn't block per D-09
</verification>

<success_criteria>
- publish wizard shows missing prerequisites with /scr: commands to fix them
- 4 presets chain correct export commands
- autopilot-publish runs voice-check + continuity-check then full pipeline
- Unattended operation with progress reporting
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-publishing/05-04-SUMMARY.md`
</output>
