---
phase: 22-runtime-docs-verification
plan: "02"
type: execute
wave: 1
depends_on:
  - "22-01"
files_modified:
  - test/installer.test.js
requirements:
  - QA-04
autonomous: true
user_setup: []
---

<objective>
Lock the runtime contract with automated coverage for the installer registry, Codex wrapper generation, and silent install parsing.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend regression coverage for the new install surface</name>
  <files>test/installer.test.js</files>
  <action>Cover silent install parsing, Codex skill naming and invocation, and Codex runtime classification so drift breaks tests instead of shipping silently.</action>
  <verify>
    <automated>node --test test/installer.test.js test/phase14-runtime-credibility.test.js test/phase16-trust-regression.test.js</automated>
  </verify>
  <done>The new runtime contract is protected by automated tests.</done>
</task>

</tasks>

<success_criteria>
- Automated tests fail if installer and doc/runtime contract drift
</success_criteria>
