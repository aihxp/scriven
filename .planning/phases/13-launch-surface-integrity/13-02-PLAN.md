---
phase: 13-launch-surface-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/scr/export.md
  - docs/publishing.md
  - docs/contributing.md
  - docs/shipped-assets.md
autonomous: true
requirements:
  - TRUST-02
  - TRUST-03
must_haves:
  truths:
    - A user following export command or publishing guidance reaches real template files or an explicit fallback instead of a dead-end file path.
    - A maintainer can open one canonical inventory and verify which export templates and launch-critical files are actually present today.
  artifacts:
    - path: commands/scr/export.md
      provides: Export instructions that reference only shipped templates or documented fallbacks.
    - path: docs/publishing.md
      provides: User-facing export guidance aligned to the actual template inventory and fallback behavior.
    - path: docs/shipped-assets.md
      provides: Canonical inventory of export templates and trust-critical launch assets.
    - path: docs/contributing.md
      provides: Contributor-facing pointer to the canonical shipped-asset inventory.
  key_links:
    - from: commands/scr/export.md
      to: data/export-templates/
      via: Explicit template paths and fallback instructions.
      pattern: scriven-book.typst|scriven-epub.css|scriven-academic.latex
    - from: docs/publishing.md
      to: docs/shipped-assets.md
      via: Canonical inventory reference.
      pattern: shipped-assets
    - from: docs/contributing.md
      to: docs/shipped-assets.md
      via: Source-of-truth guidance for export assets.
      pattern: shipped-assets
---

<objective>
Make export guidance and shipped-asset inventory trustworthy.

Purpose: Close TRUST-02 and TRUST-03 by removing dead-end export references and adding a canonical inventory of what the repo actually ships today.
Output: Export command/docs that point only to real templates or clear fallbacks, plus one inventory file for maintainers.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@commands/scr/export.md
@docs/publishing.md
@docs/contributing.md
@data/export-templates/scriven-book.typst
@data/export-templates/scriven-epub.css
@data/export-templates/scriven-academic.latex
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove export dead ends and document real fallbacks</name>
  <read_first>
commands/scr/export.md
docs/publishing.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
data/export-templates/scriven-book.typst
data/export-templates/scriven-epub.css
data/export-templates/scriven-academic.latex
  </read_first>
  <files>commands/scr/export.md, docs/publishing.md</files>
  <action>Rewrite export guidance so every referenced template path exists or is explicitly framed as an optional user-supplied fallback per TRUST-02. Remove references to absent shipped assets such as `scriven-manuscript.docx`, `scriven-formatted.docx`, and nonexistent cover-template files unless they are described as not shipped and replaced by documented behavior that the current repo can actually provide. Keep the existing export format surface and pure shell/tool architecture intact; the goal is guidance integrity, not new export features or new runtime dependencies.</action>
  <acceptance_criteria>
`commands/scr/export.md` and `docs/publishing.md` only reference real template files under `data/export-templates/` or explicit user-provided fallbacks.
No dead-end references to absent DOCX or cover-template files remain in command or publishing guidance.
KDP/Ingram guidance explains what is generated from command logic versus what is shipped as a template asset.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "scriven-manuscript\\.docx|scriven-formatted\\.docx|scriven-kdp-cover\\.typst|scriven-ingram-cover\\.typst" commands/scr/export.md docs/publishing.md && rg -n "scriven-book\\.typst|scriven-epub\\.css|scriven-academic\\.latex" commands/scr/export.md docs/publishing.md && node --test test/phase5-export-publishing.test.js</automated>
  </verify>
  <done>Users can follow export instructions from the command and publishing guide without being sent to missing shipped files.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical shipped-asset inventory and point maintainers to it</name>
  <read_first>
docs/contributing.md
README.md
package.json
commands/scr/export.md
docs/publishing.md
data/export-templates/scriven-book.typst
data/export-templates/scriven-epub.css
data/export-templates/scriven-academic.latex
  </read_first>
  <files>docs/shipped-assets.md, docs/contributing.md, docs/publishing.md</files>
  <action>Create `docs/shipped-assets.md` as the single human-readable inventory of current export templates and trust-critical launch assets per TRUST-03. Include concrete repo paths, what each file proves, and any important absence/fallback notes needed to interpret the current surface honestly. Update contributor-facing and publishing-facing docs to point to this inventory as the canonical source of truth. Do not turn the inventory into a future roadmap or aspirational wishlist; it must describe what is present now.</action>
  <acceptance_criteria>
`docs/shipped-assets.md` exists and lists the three real export templates plus the launch-critical files users and maintainers rely on.
The inventory clearly distinguishes shipped files from documented fallbacks or non-shipped assets.
`docs/contributing.md` and `docs/publishing.md` point maintainers/readers to `docs/shipped-assets.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/shipped-assets.md && rg -n "data/export-templates/scriven-book\\.typst|data/export-templates/scriven-epub\\.css|data/export-templates/scriven-academic\\.latex|README\\.md|commands/scr/export\\.md" docs/shipped-assets.md && rg -n "shipped-assets\\.md" docs/contributing.md docs/publishing.md</automated>
  </verify>
  <done>Maintainers have one canonical inventory they can inspect to confirm the actual shipped export/template surface and related launch-critical assets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → export docs/command guidance | Users trust documented file paths and fallbacks when attempting export workflows |
| maintainer → shipped-asset inventory | Maintainers need one reliable inventory rather than reconstructing the asset surface from scattered files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | D | commands/scr/export.md | mitigate | Remove dead-end file references and replace them with real template paths or explicit fallback behavior users can follow |
| T-13-05 | T | docs/publishing.md | mitigate | Keep user-facing publishing guidance aligned to the actual template directory instead of aspirational assets |
| T-13-06 | R | docs/shipped-assets.md | mitigate | Create a single canonical inventory so maintainers can verify what exists without reconciling multiple inconsistent docs |
</threat_model>

<verification>
Run the dead-end reference grep, targeted export regression test, and shipped-asset inventory grep checks from the task `<verify>` blocks.
</verification>

<success_criteria>
Export instructions no longer point to missing shipped files, and one canonical inventory now records the actual export templates and launch-critical assets present in the repo.
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-surface-integrity/13-launch-surface-integrity-02-SUMMARY.md`
</output>
