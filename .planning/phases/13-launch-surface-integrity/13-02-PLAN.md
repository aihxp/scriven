---
phase: 13-launch-surface-integrity
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - README.md
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - TRUST-01
user_setup: []
must_haves:
  truths:
    - Launch-facing root docs no longer make absolute shipped-surface claims that the repo cannot prove today.
    - Root docs use the same work-type count and shipped-template framing as the canonical asset inventory.
  artifacts:
    - README.md
    - AGENTS.md
    - CLAUDE.md
  key_links:
    - README.md status and support language align with the shipped-asset inventory created in Plan 13-01.
    - AGENTS.md and CLAUDE.md distinguish shipped export templates from planned ones using the same framing.
---

<objective>
Narrow the launch-facing root docs so the first impression matches the source of truth created in Plan 13-01.

Purpose: Even with correct export docs, the launch surface still feels untrustworthy if the README and root instruction docs overclaim what is shipped.
Output: A tighter README status/support story and root instruction docs that clearly distinguish shipped assets from planned ones.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-launch-surface-integrity/13-01-SUMMARY.md
@README.md
@AGENTS.md
@CLAUDE.md
@docs/shipped-assets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten README launch and status claims</name>
  <files>README.md</files>
  <read_first>README.md, docs/shipped-assets.md, .planning/ROADMAP.md</read_first>
  <action>Update `README.md` so the launch-facing narrative is provable from the repo surface. Replace the `All features shipped` status line with wording that acknowledges the core command surface is shipped while trust-and-proof hardening is ongoing. Remove the per-runtime `(full support)` labels and replace them with neutral install-target wording that does not imply verified parity; keep `Claude Code` as primary if helpful, but avoid unsupported certainty. Preserve Scriven's core voice-fidelity positioning and 46-work-type count. Add one doc link to `docs/shipped-assets.md` in the documentation section or a nearby trust-relevant section so users can inspect the canonical shipped-asset inventory.</action>
  <verify>rg -n "All features shipped|full support|docs/shipped-assets\\.md|46 work types" README.md</verify>
  <acceptance_criteria>
    - `README.md` does not contain `All features shipped`
    - `README.md` does not contain `(full support)`
    - `README.md` contains `docs/shipped-assets.md`
    - `README.md` contains `46 work types`
  </acceptance_criteria>
  <done>README no longer overclaims the shipped surface and points readers to the canonical inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Align AGENTS and CLAUDE root docs with shipped-versus-planned template truth</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <read_first>AGENTS.md, CLAUDE.md, docs/shipped-assets.md</read_first>
  <action>Update both `AGENTS.md` and `CLAUDE.md` to remove stale `50+ work types` wording in favor of `46 work types`, and replace the `## Template Files Scriven Should Ship` section with wording that clearly distinguishes `currently shipped export templates` from `planned export templates` or `future export templates`. The shipped section must list only `scriven-book.typst`, `scriven-epub.css`, and `scriven-academic.latex`. If you keep the DOCX or cover-template entries, move them under an explicitly unshipped/planned heading so they cannot be mistaken for bundled assets.</action>
  <verify>rg -n "46 work types|Currently Shipped Export Templates|Planned Export Templates|scriven-book\\.typst|scriven-epub\\.css|scriven-academic\\.latex" AGENTS.md CLAUDE.md</verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `46 work types`
    - `CLAUDE.md` contains `46 work types`
    - `AGENTS.md` contains `Currently Shipped Export Templates`
    - `CLAUDE.md` contains `Currently Shipped Export Templates`
    - Neither file presents `scriven-manuscript.docx` or `scriven-formatted.docx` as currently shipped templates
  </acceptance_criteria>
  <done>Root instruction docs now match the shipped-template truth and no longer reinforce the launch trust gap.</done>
</task>

<task type="auto">
  <name>Task 3: Run a root-doc consistency sweep against the new source of truth</name>
  <files>README.md, AGENTS.md, CLAUDE.md</files>
  <read_first>README.md, AGENTS.md, CLAUDE.md, docs/shipped-assets.md</read_first>
  <action>Perform a final consistency sweep across `README.md`, `AGENTS.md`, and `CLAUDE.md` so the same repo facts are expressed consistently: 46 work types, no absolute shipped-surface wording, and shipped-template framing that matches `docs/shipped-assets.md`. Fix any leftover root-level wording that would still make a first-time reader believe missing templates are bundled today.</action>
  <verify>rg -n "50\\+ work types|All features shipped|Template Files Scriven Should Ship" README.md AGENTS.md CLAUDE.md</verify>
  <acceptance_criteria>
    - `README.md`, `AGENTS.md`, and `CLAUDE.md` do not contain `All features shipped`
    - `README.md`, `AGENTS.md`, and `CLAUDE.md` do not contain `50+ work types`
    - `AGENTS.md` does not contain `Template Files Scriven Should Ship`
    - `CLAUDE.md` does not contain `Template Files Scriven Should Ship`
  </acceptance_criteria>
  <done>The root-doc launch surface presents a consistent, provable story from first read through internal instructions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "All features shipped|\\(full support\\)" README.md` returns no matches
- [ ] `rg -n "50\\+ work types|Template Files Scriven Should Ship" AGENTS.md CLAUDE.md` returns no matches
- [ ] `rg -n "Currently Shipped Export Templates|46 work types" AGENTS.md CLAUDE.md README.md` returns matches
</verification>

<success_criteria>

- All tasks completed
- README launch/status language is narrowed to what the repo can prove today
- AGENTS.md and CLAUDE.md distinguish shipped export templates from planned ones
- Root docs consistently use the 46-work-type count and the same shipped-surface framing
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-surface-integrity/13-02-SUMMARY.md`
</output>
