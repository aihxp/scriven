---
phase: 13-launch-surface-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
autonomous: true
requirements:
  - TRUST-01
must_haves:
  truths:
    - A first-time visitor sees only top-level claims that can be backed by files, commands, docs, or templates already present in the repo.
    - The launch surface leads with Voice DNA and repo-provable workflow breadth instead of unsupported support-level or completion claims.
  artifacts:
    - path: README.md
      provides: Launch-facing product overview and status language aligned to the live repo surface.
    - path: package.json
      provides: npm-facing metadata that does not overpromise beyond the repo-backed launch surface.
  key_links:
    - from: README.md
      to: commands/scr/
      via: Command-count and capability summaries.
      pattern: 101 commands
    - from: README.md
      to: data/export-templates/
      via: Export and publishing summaries.
      pattern: scriven-book.typst|scriven-epub.css|scriven-academic.latex
    - from: package.json
      to: README.md
      via: Matching top-level positioning and scope language.
      pattern: creative writing|publishing|translation
---

<objective>
Make the npm and README launch surface provable.

Purpose: Close TRUST-01 by removing or qualifying launch-facing claims that the repo cannot currently prove, while preserving Scriven's Voice DNA wedge.
Output: A narrowed README and matching package metadata that describe only what the current repo surface can support.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md
@package.json
@commands/scr/export.md
@docs/publishing.md
@data/export-templates/scriven-book.typst
@data/export-templates/scriven-epub.css
@data/export-templates/scriven-academic.latex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow README launch claims to repo-backed proof</name>
  <read_first>
README.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
commands/scr/export.md
docs/publishing.md
data/export-templates/scriven-book.typst
data/export-templates/scriven-epub.css
data/export-templates/scriven-academic.latex
  </read_first>
  <files>README.md</files>
  <action>Rewrite the README hero, "What is this", publishing summary, supported-agent summary, and status sections so every claim is traceable to files or command surfaces already present in the repo per TRUST-01. Keep the Voice DNA value proposition prominent, preserve accurate counts such as 101 commands and 46 work types when they are source-backed, and remove or qualify unsupported phrases like "full support", "all features shipped", or other launch language that implies proof artifacts or runtime verification not yet shipped. Do not solve Phase 14 runtime-matrix work or Phase 15 proof-artifact positioning inside this plan.</action>
  <acceptance_criteria>
`README.md` no longer contains unsupported support-level or completion language.
`README.md` still contains Voice DNA / STYLE-GUIDE-backed positioning and only cites counts or export claims that can be traced to live repo files.
Every docs link referenced from `README.md` resolves to an existing file.
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && rg -n "Voice DNA|STYLE-GUIDE\\.md|101 commands|46 work types" README.md && ! rg -n "full support|All features shipped|Runs on 9 AI coding agent runtimes" README.md && python - <<'PY'
from pathlib import Path
import re
text = Path('README.md').read_text()
links = re.findall(r'\((docs/[^)]+\.md)\)', text)
missing = [p for p in links if not Path(p).exists()]
if missing:
    raise SystemExit(f"missing links: {missing}")
PY</automated>
  </verify>
  <done>README is a repo-provable launch document that no longer overstates support, completion, or export proof.</done>
</task>

<task type="auto">
  <name>Task 2: Align npm package metadata with the narrowed launch surface</name>
  <read_first>
package.json
README.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
  </read_first>
  <files>package.json</files>
  <action>Update `package.json` description and any launch-signaling metadata that materially affects the npm surface so it matches the narrowed README posture per TRUST-01. Keep the package name, version, scripts, bin wiring, zero-dependency architecture, and packaging scope intact. Do not change the Node engine floor here; Phase 14 owns the runtime baseline decision.</action>
  <acceptance_criteria>
`package.json` description does not promise proof or support levels beyond the current repo surface.
`package.json` remains valid JSON and preserves the existing CLI/install wiring.
The metadata language is directionally consistent with the updated README.
  </acceptance_criteria>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
if (pkg.name !== 'scriven-cli') throw new Error('package name changed unexpectedly');
if (!pkg.bin || pkg.bin.scriven !== './bin/install.js') throw new Error('CLI wiring changed unexpectedly');
if (!pkg.description) throw new Error('description missing');
if (/full support|all features shipped/i.test(pkg.description)) throw new Error('overstated package description');
NODE</automated>
  </verify>
  <done>`package.json` presents the same proof-backed launch posture as the README without altering runtime or packaging behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → README/package metadata | Untrusted first impressions are formed from top-level copy before users inspect source files |
| launch copy → repo artifacts | Claims are only trustworthy if they can be traced to commands, docs, templates, or installer entries that actually exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | README.md | mitigate | Remove or qualify launch claims that cannot be traced to existing commands, docs, templates, or installer entries |
| T-13-02 | R | package.json + README.md | mitigate | Keep npm metadata and README positioning aligned so maintainers can defend one consistent launch story |
| T-13-03 | I | README supported-agent/status sections | mitigate | Avoid support-level language that implies runtime verification or proof artifacts before Phase 14 and Phase 15 ship |
</threat_model>

<verification>
Run the README/package checks in the task `<verify>` blocks, then skim the rendered README sections to confirm the lead message is still Voice DNA first rather than generic breadth marketing.
</verification>

<success_criteria>
Top-level launch copy is limited to repo-provable facts, the Voice DNA wedge remains visible, and the npm metadata no longer overstates support or completion.
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-surface-integrity/13-launch-surface-integrity-01-SUMMARY.md`
</output>
