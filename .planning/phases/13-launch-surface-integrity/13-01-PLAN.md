---
phase: 13-launch-surface-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/shipped-assets.md
  - commands/scr/export.md
  - docs/publishing.md
  - docs/contributing.md
autonomous: true
requirements:
  - TRUST-02
  - TRUST-03
user_setup: []
must_haves:
  truths:
    - Export command guidance and publishing docs reference only shipped export templates or explicit documented fallbacks.
    - The repo contains one canonical document listing the export templates and trust-critical launch assets that are actually shipped today.
  artifacts:
    - docs/shipped-assets.md
    - commands/scr/export.md
    - docs/publishing.md
    - docs/contributing.md
  key_links:
    - docs/shipped-assets.md matches the files present in data/export-templates/.
    - commands/scr/export.md and docs/publishing.md describe the same DOCX template behavior.
---

<objective>
Create a canonical shipped-asset inventory and align export-facing docs with the actual repo surface.

Purpose: Phase 13 cannot tighten launch claims unless the repo first has one explicit source of truth for shipped assets and export-template behavior.
Output: A new shipped-assets inventory plus corrected export command and publishing/contributor guidance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/scr/export.md
@docs/publishing.md
@docs/contributing.md
@data/export-templates/scriven-book.typst
@data/export-templates/scriven-epub.css
@data/export-templates/scriven-academic.latex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical shipped-asset inventory</name>
  <files>docs/shipped-assets.md</files>
  <read_first>commands/scr/export.md, docs/publishing.md, docs/contributing.md, data/export-templates/scriven-book.typst, data/export-templates/scriven-epub.css, data/export-templates/scriven-academic.latex</read_first>
  <action>Create `docs/shipped-assets.md` as the canonical inventory for trust-critical launch artifacts. Include these exact sections: `# Shipped Assets`, `## Export Templates Shipped Today`, `## Export Template Behaviors`, and `## Trust-Critical Launch Files`. In `## Export Templates Shipped Today`, list only `scriven-book.typst`, `scriven-epub.css`, and `scriven-academic.latex` from `data/export-templates/`. In `## Export Template Behaviors`, explicitly state that manuscript and formatted DOCX exports currently rely on Pandoc defaults unless the user supplies their own reference document, and note that `scriven-manuscript.docx`, `scriven-formatted.docx`, `scriven-kdp-cover.typst`, and `scriven-ingram-cover.typst` are not shipped today. In `## Trust-Critical Launch Files`, include at least `README.md`, `commands/scr/export.md`, `docs/publishing.md`, `docs/contributing.md`, `AGENTS.md`, and `CLAUDE.md` with one-line descriptions of why each affects launch trust.</action>
  <verify>rg -n "Export Templates Shipped Today|scriven-book\\.typst|scriven-epub\\.css|scriven-academic\\.latex|not shipped today|Trust-Critical Launch Files" docs/shipped-assets.md</verify>
  <acceptance_criteria>
    - `docs/shipped-assets.md` contains `## Export Templates Shipped Today`
    - `docs/shipped-assets.md` contains `scriven-book.typst`, `scriven-epub.css`, and `scriven-academic.latex`
    - `docs/shipped-assets.md` contains `scriven-manuscript.docx` and the phrase `not shipped today`
    - `docs/shipped-assets.md` contains `AGENTS.md` and `CLAUDE.md` under `## Trust-Critical Launch Files`
  </acceptance_criteria>
  <done>Canonical shipped-asset inventory exists and documents the real export-template surface.</done>
</task>

<task type="auto">
  <name>Task 2: Align export command and publishing guide to shipped template behavior</name>
  <files>commands/scr/export.md, docs/publishing.md</files>
  <read_first>commands/scr/export.md, docs/publishing.md, docs/shipped-assets.md</read_first>
  <action>Rewrite the DOCX-related sections in `commands/scr/export.md` and `docs/publishing.md` so they no longer present `data/export-templates/scriven-manuscript.docx` or `data/export-templates/scriven-formatted.docx` as shipped defaults. In `commands/scr/export.md`, change the EXP-02 and EXP-03 guidance so the primary documented command path produces DOCX without `--reference-doc=...` and explains that a user-supplied Pandoc reference doc is optional rather than bundled. Keep existing PDF, EPUB, and LaTeX template paths intact because those files exist. In `docs/publishing.md`, update both DOCX sections to say the current shipped experience uses Pandoc default styling for manuscript and formatted DOCX unless the user adds a custom reference doc, and ensure no sentence implies bundled DOCX reference documents already exist.</action>
  <verify>rg -n "reference-doc=data/export-templates/scriven-(manuscript|formatted)\\.docx|Pandoc default|custom reference document|not shipped" commands/scr/export.md docs/publishing.md</verify>
  <acceptance_criteria>
    - `commands/scr/export.md` no longer contains `--reference-doc=data/export-templates/scriven-manuscript.docx`
    - `commands/scr/export.md` no longer contains `--reference-doc=data/export-templates/scriven-formatted.docx`
    - `docs/publishing.md` contains `Pandoc` and `default` in both DOCX sections
    - `docs/publishing.md` does not claim bundled DOCX reference documents are shipped
  </acceptance_criteria>
  <done>Export command and publishing guide now match the actual shipped template surface.</done>
</task>

<task type="auto">
  <name>Task 3: Point contributor guidance at the canonical inventory</name>
  <files>docs/contributing.md</files>
  <read_first>docs/contributing.md, docs/shipped-assets.md</read_first>
  <action>Update the `Adding an Export Format` section in `docs/contributing.md` so the list of existing export templates matches the exact three files in `docs/shipped-assets.md`, and add a sentence stating that `docs/shipped-assets.md` is the canonical inventory for shipped export templates and trust-critical launch assets. Do not leave any wording that implies additional DOCX or cover templates are already bundled.</action>
  <verify>rg -n "docs/shipped-assets\\.md|scriven-book\\.typst|scriven-epub\\.css|scriven-academic\\.latex" docs/contributing.md</verify>
  <acceptance_criteria>
    - `docs/contributing.md` contains `docs/shipped-assets.md`
    - `docs/contributing.md` contains `scriven-book.typst`, `scriven-epub.css`, and `scriven-academic.latex`
    - `docs/contributing.md` does not mention `scriven-manuscript.docx`
    - `docs/contributing.md` does not mention `scriven-formatted.docx`
  </acceptance_criteria>
  <done>Contributor-facing template guidance now points to the same canonical inventory as the launch surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Export Templates Shipped Today|Trust-Critical Launch Files" docs/shipped-assets.md` returns matches
- [ ] `rg -n "reference-doc=data/export-templates/scriven-(manuscript|formatted)\\.docx" commands/scr/export.md` returns no matches
- [ ] `rg -n "docs/shipped-assets\\.md" docs/contributing.md` returns a match
</verification>

<success_criteria>

- All tasks completed
- `docs/shipped-assets.md` exists as the canonical shipped-asset inventory
- Export command and publishing docs describe only shipped templates or explicit fallbacks
- Contributor docs no longer imply nonexistent export templates are bundled
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-surface-integrity/13-01-SUMMARY.md`
</output>
