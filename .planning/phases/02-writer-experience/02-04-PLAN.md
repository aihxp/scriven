---
phase: 02-writer-experience
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - test/phase2-writer-mode.test.js
  - test/phase2-autopilot.test.js
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05, AUTO-06, AUTO-07, AUTO-08, AUTO-09, AUTO-10, AUTO-11, AUTO-12, AUTO-13]

must_haves:
  truths:
    - "All 9 new command files pass structural validation (frontmatter, heading, description)"
    - "Config template has autopilot.custom_checkpoints and developer_mode fields"
    - "STATE.md template has session metrics section"
    - "CONSTRAINTS.json has all 9 Phase 2 commands registered"
    - "Autopilot command references all three profiles"
    - "Writer git commands never mention git terminology in their writer-mode sections"
  artifacts:
    - path: "test/phase2-writer-mode.test.js"
      provides: "Tests for writer mode config, git command content, session state"
      contains: "developer_mode"
    - path: "test/phase2-autopilot.test.js"
      provides: "Tests for autopilot profiles, config schema, pause conditions"
      contains: "custom_checkpoints"
  key_links:
    - from: "test/phase2-writer-mode.test.js"
      to: "templates/config.json"
      via: "Validates config schema has required fields"
      pattern: "config\\.json"
    - from: "test/phase2-autopilot.test.js"
      to: "commands/scr/autopilot.md"
      via: "Validates autopilot command content"
      pattern: "autopilot\\.md"
---

<objective>
Create Phase 2 test suite verifying all 9 new commands, config schema, and template updates. Tests confirm structural correctness and content requirements from user decisions.

Purpose: Automated validation that all Phase 2 artifacts meet the specifications from D-01 through D-12.
Output: Two test files covering writer mode and autopilot requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-writer-experience/02-CONTEXT.md

@test/commands.test.js
@templates/config.json
@templates/STATE.md

<interfaces>
<!-- Existing test pattern from test/commands.test.js: -->
<!-- Uses node:test (describe, it) + node:assert/strict -->
<!-- Reads files with fs.readFileSync, checks with assert.match/assert.ok -->
<!-- No npm dependencies -- pure Node.js built-in test runner -->

<!-- Phase 2 commands created by plans 01-03: -->
<!-- commands/scr/autopilot.md -->
<!-- commands/scr/save.md, history.md, compare.md, versions.md, undo.md -->
<!-- commands/scr/pause-work.md, resume-work.md, session-report.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create writer mode test suite</name>
  <files>test/phase2-writer-mode.test.js</files>
  <read_first>
    - test/commands.test.js (existing test pattern -- follow same style)
    - test/constraints.test.js (pattern for CONSTRAINTS.json tests)
    - templates/config.json (fields to validate)
    - templates/STATE.md (sections to validate)
    - .planning/phases/02-writer-experience/02-CONTEXT.md (D-05 through D-12 decisions)
  </read_first>
  <action>
Create `test/phase2-writer-mode.test.js` using Node.js built-in test runner (node:test). Follow the exact pattern from commands.test.js (require node:test describe/it, require node:assert/strict, use fs to read files).

**Test groups:**

1. **"Config template schema"** -- tests on templates/config.json:
   - `developer_mode` field exists and defaults to `false`
   - `autopilot.custom_checkpoints` field exists and is an array
   - `autopilot.profile` field exists and defaults to "guided"
   - `autopilot.enabled` field exists and defaults to `false`
   - Config file is valid JSON (JSON.parse does not throw)

2. **"STATE.md template"** -- tests on templates/STATE.md:
   - Contains "## Session metrics" section
   - Contains "## Session handoff" section
   - Contains "## Last actions" section with table header
   - Contains "## Progress" section
   - Session metrics section has SESSION_START, SESSION_WORDS, SESSION_UNITS placeholders

3. **"Writer git commands content"** -- tests on each of save.md, history.md, compare.md, versions.md, undo.md:
   - Each file exists in commands/scr/
   - Each has YAML frontmatter with `description:` field
   - Each references `developer_mode` or `writer mode` (handles output formatting)
   - save.md contains "Saved after drafting" (auto-generated message pattern per D-05)
   - history.md contains "Date" and "Action" and "Details" (table columns per D-06)
   - compare.md contains "Before" and "After" (prose diff format per D-07)
   - compare.md contains "NEVER" and one of "+", "-", "@@" (anti-pattern instruction per D-07)
   - undo.md contains "You'll lose" or "will lose" (confirmation prompt per D-08)
   - undo.md contains "unsaved" or "uncommitted" (safety check)

4. **"Session commands content"** -- tests on pause-work.md, resume-work.md, session-report.md:
   - Each file exists in commands/scr/
   - Each has YAML frontmatter with `description:` field
   - pause-work.md contains "notes" and "come back" (per D-09 mental note capture)
   - resume-work.md contains "Last time" or "last time" (per D-10 one-paragraph format)
   - session-report.md contains "words" and "units" (per D-11 metrics)

5. **"CONSTRAINTS.json has session-report"** -- test on data/CONSTRAINTS.json:
   - JSON.parse succeeds
   - `commands["session-report"]` exists
   - `commands["session-report"].category` equals "session"
   - `commands["session-report"].available` includes "all"
  </action>
  <verify>
    <automated>node --test test/phase2-writer-mode.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/phase2-writer-mode.test.js exists
    - Uses `require('node:test')` and `require('node:assert/strict')` (matching project pattern)
    - Has at least 15 individual test cases across the 5 groups
    - Tests config.json for developer_mode and custom_checkpoints
    - Tests STATE.md for Session metrics section
    - Tests each git command file for decision-specific content (D-05 through D-08)
    - Tests each session command for decision-specific content (D-09 through D-11)
    - Tests CONSTRAINTS.json for session-report entry
    - All tests pass when run with `node --test test/phase2-writer-mode.test.js`
  </acceptance_criteria>
  <done>Writer mode test suite validates all config/template/command requirements from D-05 through D-12</done>
</task>

<task type="auto">
  <name>Task 2: Create autopilot test suite</name>
  <files>test/phase2-autopilot.test.js</files>
  <read_first>
    - test/commands.test.js (existing test pattern)
    - test/phase2-writer-mode.test.js (just created -- follow same style)
    - commands/scr/autopilot.md (content to validate)
    - .planning/phases/02-writer-experience/02-CONTEXT.md (D-01 through D-04 decisions)
  </read_first>
  <action>
Create `test/phase2-autopilot.test.js` using Node.js built-in test runner. Follow the exact pattern from commands.test.js.

**Test groups:**

1. **"Autopilot command structure"** -- tests on commands/scr/autopilot.md:
   - File exists
   - Has YAML frontmatter with `description:` and `argument-hint:` fields
   - argument-hint contains `--profile` and `--resume`
   - Has "# Autopilot" heading

2. **"Autopilot profiles"** -- content tests on commands/scr/autopilot.md:
   - Contains the word "guided" (profile name)
   - Contains the word "supervised" (profile name)
   - Contains "full-auto" (profile name)
   - Contains "approve" and "revise" and "stop" (guided review options per D-01)
   - Contains "200 words" or "last 200" (guided shows last 200 words per D-01)
   - Contains "structural unit" or "mid" level reference (supervised batching per D-04)
   - Contains "custom_checkpoints" (full-auto writer-defined checkpoints per D-02)

3. **"Autopilot pause conditions"** -- content tests on commands/scr/autopilot.md:
   - Contains "continuity" (pause condition 1)
   - Contains "voice drift" or "drift" (pause condition 2)
   - Contains "plot hole" (pause condition 3)
   - Contains "missing" and ("information" or "info") (pause condition 4)

4. **"Autopilot resume"** -- content tests on commands/scr/autopilot.md:
   - Contains "--resume" (resume flag per D-03)
   - Contains "STATE.md" (reads state for resume)
   - Contains "one sentence" or "where it left off" or "picking up" (per D-03 one-sentence explanation)

5. **"Autopilot state management"** -- content tests:
   - Contains "Update STATE.md" or "update STATE.md" (mandatory state updates)
   - Contains "OUTLINE.md" (reads outline for unit enumeration)
   - Contains "config.json" or "config" (reads autopilot settings)
  </action>
  <verify>
    <automated>node --test test/phase2-autopilot.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/phase2-autopilot.test.js exists
    - Uses `require('node:test')` and `require('node:assert/strict')`
    - Has at least 15 individual test cases across the 5 groups
    - Validates all three profile names present in autopilot.md
    - Validates D-01 specifics (200 words, approve/revise/stop)
    - Validates D-02 specifics (custom_checkpoints, 4 pause conditions)
    - Validates D-03 specifics (--resume flag, STATE.md, one-sentence explanation)
    - Validates D-04 specifics (structural unit boundary)
    - All tests pass when run with `node --test test/phase2-autopilot.test.js`
  </acceptance_criteria>
  <done>Autopilot test suite validates all profile behaviors, pause conditions, and resume logic from D-01 through D-04</done>
</task>

</tasks>

<verification>
- Both test files pass: `node --test test/phase2-writer-mode.test.js test/phase2-autopilot.test.js`
- Full existing test suite still passes: `node --test test/*.test.js`
- At least 30 total test cases across both files
</verification>

<success_criteria>
- All Phase 2 requirements (AUTO-01 through AUTO-13) have at least one test case
- All user decisions (D-01 through D-12) have content-verification tests
- Tests run in under 5 seconds
- Zero test failures on green codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-writer-experience/02-04-SUMMARY.md`
</output>
