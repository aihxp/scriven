---
phase: 01-mvp-polish
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - test/demo.test.js
  - test/package.test.js
  - package.json
autonomous: false
requirements: [MVP-05, MVP-06]
must_haves:
  truths:
    - "Demo completeness test validates all expected files exist and are non-empty"
    - "Package test validates npm publishability: bin, shebang, engines, files, publishConfig"
    - "npm pack includes all expected files including data/demo/.manuscript/ contents"
    - "npx scriven experience is verified through dry-run testing"
  artifacts:
    - path: "test/demo.test.js"
      provides: "Demo file completeness validation"
      contains: "data/demo"
    - path: "test/package.test.js"
      provides: "npm package publishability validation"
      contains: "publishConfig"
    - path: "package.json"
      provides: "Final polished package.json"
      contains: "prepublishOnly"
  key_links:
    - from: "test/demo.test.js"
      to: "data/demo/.manuscript/"
      via: "fs.existsSync checks for every expected demo file"
      pattern: "data/demo"
    - from: "test/package.test.js"
      to: "package.json"
      via: "JSON.parse and field assertions"
      pattern: "package.json"
    - from: "test/package.test.js"
      to: "npm pack"
      via: "execSync captures tarball file list"
      pattern: "npm pack"
---

<objective>
Create demo completeness tests (validates Plan 01 output) and package publishability tests (validates the full npm package). Run end-to-end verification that npm pack includes everything. Final checkpoint: human verifies the demo reads well.

Purpose: This plan closes the loop -- Plan 01 created demo content, Plan 02 created tests for existing code, and now Plan 03 validates the demo content and the overall package. Per D-06 (test area 4: demo file completeness) and D-09 (package needs).
Output: 2 test files, polished package.json, human verification of demo quality
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-polish/01-CONTEXT.md
@.planning/phases/01-mvp-polish/01-RESEARCH.md
@.planning/phases/01-mvp-polish/01-01-SUMMARY.md
@.planning/phases/01-mvp-polish/01-02-SUMMARY.md

<interfaces>
<!-- Expected demo file structure from Plan 01 -->
Expected files under data/demo/.manuscript/:
  WORK.md, BRIEF.md, OUTLINE.md, STATE.md, STYLE-GUIDE.md,
  CHARACTERS.md, PLOT-GRAPH.md, THEMES.md, config.json,
  drafts/body/1-the-letter-DRAFT.md,
  drafts/body/2-the-workshop-DRAFT.md,
  drafts/body/3-the-pier-DRAFT.md,
  drafts/body/4-the-clock-DRAFT.md,
  plans/5-the-reunion-PLAN.md,
  reviews/2-the-workshop-REVIEW.md

<!-- From package.json (current state after Plan 02 updates) -->
  name: "scriven"
  bin: { "scriven": "./bin/install.js" }
  engines: { "node": ">=18.0.0" }
  files: ["bin/", "lib/", "data/", "commands/", "agents/", "templates/", "README.md", "LICENSE"]
  scripts: { "test": "node --test test/*.test.js", "prepublishOnly": "npm test" }
  publishConfig: { "access": "public" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo completeness and package publishability tests</name>
  <files>
    test/demo.test.js
    test/package.test.js
  </files>
  <read_first>
    package.json
    bin/install.js (first line -- verify shebang)
    data/demo/.manuscript/STATE.md (verify it exists from Plan 01)
  </read_first>
  <action>
    **test/demo.test.js (MVP-01 -- demo file completeness per D-06 area 4):**

    Using `node:test` and `node:assert/strict`:

    1. Define the complete expected file list as an array:
    ```javascript
    const EXPECTED_FILES = [
      'WORK.md', 'BRIEF.md', 'OUTLINE.md', 'STATE.md',
      'STYLE-GUIDE.md', 'CHARACTERS.md', 'PLOT-GRAPH.md',
      'THEMES.md', 'config.json',
      'drafts/body/1-the-letter-DRAFT.md',
      'drafts/body/2-the-workshop-DRAFT.md',
      'drafts/body/3-the-pier-DRAFT.md',
      'drafts/body/4-the-clock-DRAFT.md',
      'plans/5-the-reunion-PLAN.md',
      'reviews/2-the-workshop-REVIEW.md',
    ];
    ```

    2. `describe('demo project completeness')`:
       - `it('all expected demo files exist')` -- for each file in EXPECTED_FILES, assert `fs.existsSync(path.join(demoDir, file))` where `demoDir = path.join(__dirname, '..', 'data', 'demo', '.manuscript')`
       - `it('all demo files are non-empty')` -- for each file, assert `fs.statSync(filePath).size > 0`
       - `it('STYLE-GUIDE.md has no unfilled placeholders')` -- read content, assert it does NOT match `/\{\{[A-Z_]+\}\}/` (no `{{PLACEHOLDER}}` tokens)
       - `it('STATE.md references 4 of 5 drafted')` -- read content, assert it contains "4 of 5" or "4/5"
       - `it('config.json is valid JSON with work_type')` -- JSON.parse succeeds, result has `work_type` key equal to `"short_story"`
       - `it('drafted scenes have substantial content')` -- for each of the 4 DRAFT.md files, assert word count (split on /\s+/) is >= 500
       - `it('scene 5 is a plan, not a draft')` -- read 5-the-reunion-PLAN.md, assert it does NOT start with prose-like paragraph (assert it contains "Scene goal" or "Emotional arc" or "Key beats")

    **test/package.test.js (MVP-05, MVP-06):**

    Using `node:test`, `node:assert/strict`, and `node:child_process`:

    1. `describe('package.json fields')`:
       - `it('has correct name')` -- pkg.name === "scriven"
       - `it('has bin entry pointing to install.js')` -- pkg.bin.scriven === "./bin/install.js"
       - `it('has engines field')` -- pkg.engines.node exists and starts with ">="
       - `it('has publishConfig with public access')` -- pkg.publishConfig.access === "public"
       - `it('has files array')` -- Array.isArray(pkg.files) and pkg.files.includes("data/")
       - `it('has test script')` -- pkg.scripts.test contains "node --test"
       - `it('has prepublishOnly script')` -- pkg.scripts.prepublishOnly === "npm test"

    2. `describe('bin/install.js')`:
       - `it('has shebang line')` -- read first line of bin/install.js, assert it equals "#!/usr/bin/env node"

    3. `describe('npm pack dry-run')`:
       - `it('includes all critical directories')` -- run `execSync('npm pack --dry-run 2>&1', { encoding: 'utf-8' })`, parse the output, assert it includes lines containing "bin/install.js", "data/CONSTRAINTS.json", "commands/scr/demo.md", "templates/STYLE-GUIDE.md", "agents/"
       - `it('includes demo manuscript files')` -- from the same npm pack output, assert it includes "data/demo/.manuscript/STYLE-GUIDE.md" (this tests the dotfile inclusion concern from RESEARCH.md)
       - If dotfiles ARE excluded by npm, this test will fail, which is the correct behavior -- the executor should then investigate and fix (possibly by renaming `.manuscript` to `manuscript` in the demo directory)
  </action>
  <verify>
    <automated>node --test test/demo.test.js test/package.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/demo.test.js contains `EXPECTED_FILES` array with 15 entries
    - test/demo.test.js contains `existsSync` and `statSync` (file existence + non-empty checks)
    - test/demo.test.js contains `/\{\{[A-Z_]+\}\}/` or similar regex (placeholder detection)
    - test/package.test.js contains `publishConfig` and `access` and `public`
    - test/package.test.js contains `npm pack` (package content verification)
    - test/package.test.js contains `#!/usr/bin/env node` (shebang check)
    - Running `node --test test/demo.test.js test/package.test.js` exits with code 0
    - Running `npm test` (full suite including all 5 test files) exits with code 0
  </acceptance_criteria>
  <done>Demo completeness tests pass (all files exist, non-empty, no placeholders, STATE.md correct). Package tests pass (name, bin, engines, publishConfig, files, shebang, npm pack includes demo files).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify demo prose quality and overall package</name>
  <files>n/a</files>
  <action>
    Human verification checkpoint. All automated work is complete from Plans 01, 02, and Task 1 of this plan. The human now verifies the demo prose quality and overall package readiness. No code changes needed -- this is a quality gate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Human has approved demo prose quality and confirmed package is ready for npm publish.</done>
  <what-built>
    Complete demo project with 4 scenes of literary prose (~800-1200 words each), all context files populated, scene 5 plan, editor notes, full test suite (5 test files), and polished package.json ready for npm publish.
  </what-built>
  <how-to-verify>
    1. Read the demo story: `cat data/demo/.manuscript/drafts/body/1-the-letter-DRAFT.md` -- Does the opening scene hook you? Does Elias feel like a real character?
    2. Skim scenes 2-4 for voice consistency: Do they all feel like the same story by the same writer?
    3. Check the Style Guide: `cat data/demo/.manuscript/STYLE-GUIDE.md` -- Is it a good teaching tool? Would a new user learn what Voice DNA means?
    4. Check the characters: `cat data/demo/.manuscript/CHARACTERS.md` -- Are Elias and Petra distinct with believable voice anchors?
    5. Run the full test suite: `npm test` -- All tests pass?
    6. Verify package contents: `npm pack --dry-run` -- All expected files included?
    7. Check the editor review: `cat data/demo/.manuscript/reviews/2-the-workshop-REVIEW.md` -- Do the notes feel like real editorial feedback?
  </how-to-verify>
  <resume-signal>Type "approved" if the demo quality meets the bar for a first impression, or describe specific issues (e.g., "scene 1 prose feels flat", "style guide missing X dimension")</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes all 5 test files (constraints, commands, installer, demo, package)
- `npm pack --dry-run` shows data/demo/.manuscript/ files in the tarball
- Demo prose is human-verified for quality
- Package is ready for `npm publish` (all D-09 requirements met)
</verification>

<success_criteria>
The full test suite passes, npm pack includes all files, and a human has confirmed the demo prose quality meets the bar for a compelling first impression. The package is ready for npm publish.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-polish/01-03-SUMMARY.md`
</output>
