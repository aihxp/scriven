---
phase: 03-creative-toolkit
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - test/phase3-creative-toolkit.test.js
autonomous: true
requirements: [CHAR-01, CHAR-02, CHAR-03, CHAR-04, CHAR-05, CHAR-06, CHAR-07, CHAR-08, STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-04, STRUCT-05, STRUCT-06]

must_haves:
  truths:
    - "Test suite validates all 14 Phase 3 requirements are met"
    - "All new command files pass frontmatter and structure validation"
    - "CONSTRAINTS.json entries exist for every new command"
    - "Work-type adaptations are verified for sacred and academic types"
    - "Full test suite (all phases) passes without regressions"
  artifacts:
    - path: "test/phase3-creative-toolkit.test.js"
      provides: "Phase 3 test coverage for all CHAR and STRUCT requirements"
      exports: ["describe", "it"]
      min_lines: 100
  key_links:
    - from: "test/phase3-creative-toolkit.test.js"
      to: "commands/scr/*.md"
      via: "file existence and content checks"
      pattern: "readFileSync.*commands"
    - from: "test/phase3-creative-toolkit.test.js"
      to: "data/CONSTRAINTS.json"
      via: "JSON parse and entry validation"
      pattern: "CONSTRAINTS"
---

<objective>
Create the Phase 3 test suite that validates all 14 requirements (CHAR-01 through CHAR-08, STRUCT-01 through STRUCT-06) are met by verifying command file existence, structure, content patterns, CONSTRAINTS.json entries, and work-type adaptations.

Purpose: Ensures all Phase 3 deliverables are correct and no regressions in existing tests. This is the phase gate -- nothing ships without green tests.
Output: test/phase3-creative-toolkit.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-creative-toolkit/03-CONTEXT.md
@.planning/phases/03-creative-toolkit/03-RESEARCH.md

@test/phase2-writer-mode.test.js (test pattern to follow)
@test/commands.test.js (existing command validation)
@test/constraints.test.js (existing CONSTRAINTS.json validation)
</context>

<interfaces>
<!-- Test pattern from existing phase2-writer-mode.test.js -->
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const ROOT = path.join(__dirname, '..');
const commandsDir = path.join(ROOT, 'commands', 'scr');

<!-- Phase 3 command files to validate -->
Character commands: character-sheet.md, character-voice-sample.md, cast-list.md, character-arc.md, relationship-map.md, build-world.md
Structure commands: plot-graph.md (enhanced), timeline.md, theme-tracker.md, subplot-map.md, outline.md
Structure management: add-unit.md, insert-unit.md, remove-unit.md, split-unit.md, merge-units.md, reorder-units.md
Templates: templates/WORLD.md

<!-- Locked decisions to verify -->
D-01: Voice anchors with 5-8 attributes (speech patterns, vocabulary register, etc.)
D-02: ASCII graph with labeled edges in relationship-map
D-03: Character-arc cross-references PLOT-GRAPH.md
D-04: WORLD.md has 5 sections (Geography, Culture, Technology/Magic, Rules/Laws, History)
D-05: build-world has --area flag for progressive refinement
D-06: plot-graph has --type flag with auto-detection
D-07: Structure commands warn about drafted content, never silently delete
D-08: theme-tracker suggests but never auto-adds
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 3 test suite covering all requirements</name>
  <files>test/phase3-creative-toolkit.test.js</files>
  <read_first>
    - test/phase2-writer-mode.test.js (test pattern -- describe/it blocks, fs.readFileSync, assert patterns)
    - test/phase2-autopilot.test.js (additional test patterns)
    - test/commands.test.js (how command files are validated)
    - test/constraints.test.js (how CONSTRAINTS.json is validated)
    - data/CONSTRAINTS.json (entries to verify)
    - .planning/phases/03-creative-toolkit/03-RESEARCH.md (validation architecture section, requirement->test map)
  </read_first>
  <behavior>
    - Test: All 6 character command files exist (character-sheet, character-voice-sample, cast-list, character-arc, relationship-map, build-world)
    - Test: All 5 structure visualization command files exist (timeline, theme-tracker, subplot-map, outline, plot-graph)
    - Test: All 6 structure management command files exist (add-unit, insert-unit, remove-unit, split-unit, merge-units, reorder-units)
    - Test: All command files have valid YAML frontmatter with description field
    - Test: WORLD.md template exists with 5 section headers (Geography, Culture, Technology/Magic, Rules/Laws, History)
    - Test: character-arc.md references PLOT-GRAPH.md (D-03)
    - Test: relationship-map.md contains ASCII graph instructions (D-02)
    - Test: build-world.md contains --area flag (D-05)
    - Test: plot-graph.md contains auto-detection and all 9 arc types including kishotenketsu (D-06)
    - Test: theme-tracker.md contains suggest-not-auto-add pattern (D-08)
    - Test: Structure management commands contain draft-safety warnings (D-07)
    - Test: CONSTRAINTS.json has entries for all new commands
    - Test: CONSTRAINTS.json entries for character commands have sacred adaptations (CHAR-08)
    - Test: All command files reference CONSTRAINTS.json or file_adaptations for work-type adaptation
  </behavior>
  <action>
Create `test/phase3-creative-toolkit.test.js` using the node:test pattern from existing phase tests.

Structure the test file with these describe blocks:

**describe('Character command files'):**
- CHAR-01: Verify new-character.md has voice anchor sections (speech patterns, vocabulary, internal monologue)
- CHAR-02: Verify character-sheet.md exists, has frontmatter, has --edit flag, references file_adaptations
- CHAR-04: Verify character-voice-sample.md exists, mentions "5 lines" or "five lines" of dialogue
- CHAR-06: Verify cast-list.md exists, has roster/table format instructions

**describe('Character visualization and world commands'):**
- CHAR-03: Verify character-arc.md exists, references PLOT-GRAPH.md (D-03)
- CHAR-05: Verify relationship-map.md exists, contains ASCII graph pattern (D-02)
- CHAR-07: Verify build-world.md exists with --area flag (D-05); verify templates/WORLD.md has 5 sections (D-04)

**describe('Structure visualization commands'):**
- STRUCT-01: Verify plot-graph.md has all 9 arc types including kishotenketsu, has auto-detection (D-06)
- STRUCT-02: Verify timeline.md exists, references OUTLINE.md
- STRUCT-03: Verify theme-tracker.md exists, contains suggest-not-auto-add pattern (D-08)
- STRUCT-04: Verify subplot-map.md exists, mentions 2+ thread requirement
- STRUCT-05: Verify outline.md exists, has --edit flag, references hierarchy

**describe('Structure management commands'):**
- STRUCT-06: Verify all 6 commands exist (add-unit, insert-unit, remove-unit, split-unit, merge-units, reorder-units)
- STRUCT-06/D-07: Verify remove-unit, split-unit, merge-units, reorder-units contain draft-safety instructions (match /confirm|warn|draft/i)
- STRUCT-06: Verify all 6 reference CONSTRAINTS.json hierarchy

**describe('CONSTRAINTS.json Phase 3 entries'):**
- CHAR-08: Verify character commands have sacred adaptations (figure-sheet, figure-arc, figures-list, lineage-map, figure-voice-sample)
- CHAR-08: Verify character commands have academic adaptations where applicable (concept-sheet)
- Verify all new commands exist in CONSTRAINTS.json commands object
- Verify all new commands have required fields: category, available, adapted, requires
- Verify feature_prerequisites entries exist for commands that need them

**describe('Work-type adaptation coverage'):**
- For each new command file: verify it contains "CONSTRAINTS.json" or "file_adaptations" or "config.json" (work-type awareness)
- Verify build-world references COSMOLOGY.md adaptation for sacred
- Verify cast-list references FIGURES.md adaptation for sacred

Run: `node --test test/phase3-creative-toolkit.test.js`
Then run full suite: `node --test test/*.test.js` to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/hprincivil/Projects/scriven && node --test test/phase3-creative-toolkit.test.js && node --test test/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/phase3-creative-toolkit.test.js exists with 20+ test cases
    - File uses node:test and node:assert/strict (matching project pattern)
    - Tests cover all 14 requirements: CHAR-01 through CHAR-08, STRUCT-01 through STRUCT-06
    - Tests verify locked decisions: D-01 through D-08
    - All tests pass: `node --test test/phase3-creative-toolkit.test.js` exits 0
    - Full test suite passes: `node --test test/*.test.js` exits 0
    - No existing tests broken (regression check)
  </acceptance_criteria>
  <done>Phase 3 test suite validates all 14 requirements and 8 locked decisions, full test suite passes with no regressions</done>
</task>

</tasks>

<verification>
- `node --test test/phase3-creative-toolkit.test.js` passes all tests
- `node --test test/*.test.js` passes all tests (no regressions)
- Test count >= 20 test cases covering all requirements
</verification>

<success_criteria>
- Phase 3 test file exists and passes
- All 14 requirements have at least one test case
- All 8 locked decisions (D-01 through D-08) have verification tests
- No regressions in existing Phase 1 and Phase 2 tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-creative-toolkit/03-04-SUMMARY.md`
</output>
