---
phase: 17-perplexity-runtime-support
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - docs/runtime-support.md
  - test/installer.test.js
autonomous: true
requirements:
  - RUNTIME-05
  - RUNTIME-06
user_setup: []
must_haves:
  truths:
    - A user can choose Perplexity Desktop in the installer as a named target.
    - The installer flow for Perplexity Desktop matches a guided/manual desktop-MCP setup rather than pretending Scriven can copy commands into a Perplexity-owned runtime directory.
    - The canonical runtime-support matrix can describe the new target with evidence and support framing that match the installer behavior.
  artifacts:
    - bin/install.js
    - docs/runtime-support.md
    - test/installer.test.js
  key_links:
    - The Perplexity Desktop installer target appears in the runtime registry with properties that reflect its real setup model.
    - docs/runtime-support.md includes a canonical row for Perplexity Desktop that matches the installer registry.
    - Installer tests cover the new runtime target and its expected property shape.
---

<objective>
Add Perplexity Desktop as a truthful Scriven installer target by extending the installer registry, setup flow, and runtime-support matrix without inventing unsupported file-copy behavior.

Purpose: users should be able to pick Perplexity Desktop as a real option while still understanding that the setup path differs from command-directory and skills-manifest runtimes.
Output: a named Perplexity Desktop target in the installer, matching runtime-matrix wiring, and installer tests that protect the new setup model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-perplexity-runtime-support/17-CONTEXT.md
@.planning/phases/17-perplexity-runtime-support/17-RESEARCH.md
@bin/install.js
@docs/runtime-support.md
@test/installer.test.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the installer registry for Perplexity Desktop</name>
  <files>bin/install.js</files>
  <action>Update `bin/install.js` so Perplexity Desktop appears as a named runtime choice with a setup model that matches the documented desktop-MCP surface. Do not model it as a normal `commands` or `skills` file-copy target unless the file paths and copy behavior are genuinely real. If needed, introduce a guided/manual branch or a new runtime type that lets the installer present prerequisites, setup guidance, and next steps honestly. Preserve the existing Node baseline and avoid adding runtime dependencies.</action>
  <verify>
    <automated>node --test test/installer.test.js</automated>
  </verify>
  <done>The installer presents Perplexity Desktop as a truthful target whose setup flow matches the real platform surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the runtime matrix and installer tests to the new target</name>
  <files>docs/runtime-support.md, test/installer.test.js</files>
  <action>Update the canonical runtime matrix in `docs/runtime-support.md` so Perplexity Desktop has an install type, repo evidence, support level, and verification status that match the new installer behavior. Extend `test/installer.test.js` to cover the new target's registry shape, ordering expectations if relevant, and any new install-type semantics introduced for Perplexity. Keep the wording conservative and aligned with the trust model already established in v1.3.</action>
  <verify>
    <automated>node --test test/installer.test.js test/phase16-trust-regression.test.js</automated>
  </verify>
  <done>The runtime matrix and installer tests both protect the same Perplexity Desktop setup model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer option -> user expectation | The label and setup flow in `bin/install.js` shape what users believe Scriven can install into. |
| runtime matrix -> support claim | `docs/runtime-support.md` is the canonical statement of install type, evidence, and verification status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | T | `bin/install.js` | mitigate | Model Perplexity Desktop using a setup type that matches its real surface rather than misclassifying it as a file-copy runtime. |
| T-17-01-02 | R | `docs/runtime-support.md` | mitigate | Keep matrix wording aligned to the installer target and conservative about parity. |
| T-17-01-03 | R | `test/installer.test.js` | mitigate | Extend installer tests to enforce the new runtime's registry shape and semantics. |
</threat_model>

<verification>
- `node --test test/installer.test.js` passes
- `node --test test/phase16-trust-regression.test.js` passes with Perplexity Desktop included in the runtime-support matrix checks
</verification>

<success_criteria>
- Perplexity Desktop exists as a named installer target
- The installer flow tells the truth about how setup works
- The runtime-support matrix and installer tests stay aligned with the new target
</success_criteria>

<output>
After completion, create `.planning/phases/17-perplexity-runtime-support/17-01-SUMMARY.md`
</output>
